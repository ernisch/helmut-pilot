.env
.env.local
env.local
env.local.html
.helmut-data/
.vercel
# Isolierte Shadow-Ablage (Quellenarchitektur): ephemere, DB-freie Artefakte — NIE ins Git.
shadow-store/
# Betreiber-Backups (scripts/backup-export.js): enthalten personenbezogene/
# politische Daten (Art. 9 DSGVO) — NIE committen.
backups/

# Temp-/Build-Artefakte (nie tracken)
node_modules/
tmp/
output/
.DS_Store
.helmut-mailvorschau/

# Lambda-Bauartefakte (Korrekturlauf 2026-08-14/3): das Archiv wird aus dem Repository
# GEBAUT (scripts/lambda-paket-bauen.js), nie committet — Binaerarchive gehoeren nicht in
# die Versionsverwaltung.
build/
