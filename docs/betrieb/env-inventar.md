# Environment-Inventar — vollständige Referenz

**Stand: 2026-08-24** (Härtungssprint Selbstweck: §7a neu — fünf statt drei Werte für die Umschaltung; Production-Zustand von `HELMUT_SCALABLE_PIPELINE`/`HELMUT_JOB_DISPATCH_MODE` und die Anwendung der OP-30-Migrationen berichtigt). Erstfassung 2026-07-15, Folgebranch.

**Zweck (Audit-Blocker):** Der Betrieb hing an einem Bus-Faktor 1 auch auf
Konfigurationsebene — `.env.example` deckte nur ~35 von ~100 im Code gelesenen
Variablen ab. Diese Datei listet **alle** aus dem Code (`server.js`, `api/`,
`lib/helmut/**`, `sw.js`) tatsächlich gelesenen Variablen mit Zweck, Default,
Secret-Status und Pflicht/optional. Automatisch gegen den Code abgeglichen; ein
Regressionstest (`scripts/env-inventar-test.js`) hält die Liste synchron —
inklusive der dynamisch gelesenen Variablen (`envList("…")`, `flagValue("…")`,
`env.NAME`-Parameter).

**Bedienung:** Secrets stehen NUR in Vercel (Project → Settings → Environment
Variables), niemals im Repo. Bei jeder Änderung: hier Ist-Wert des Betreibers
NICHT eintragen (nur Zweck/Default), sondern in den Passwort-Manager. Nach
Neuaufbau des Systems ist diese Liste die Rekonstruktionsgrundlage.
Rotationsdetails je Secret: `docs/betrieb/secret-rotation.md`.

Legende: **S** = Secret (nie loggen/committen) · **P** = Pflicht in Production ·
**O** = optional (Default greift). **Merkmale** (kompakt, wo sinnvoll):
Lesestelle · Umgebung (Prod = nur Vercel Production nötig; alle = Dev/Preview/Prod
gleichbedeutsam) · Fail-Verhalten bei Fehlen/Fehlwert (fail-open = Feature fällt
weg, App läuft; fail-closed = Zugriff/Aktion wird verweigert) · Rotationsbedarf.

## 1. Secrets / Zugang (S)

| Variable | P/O | Zweck / Default | Merkmale |
|---|---|---|---|
| `SUPABASE_URL` | P | Supabase-Projekt-URL. Ohne: V3-Store inert, App läuft Blob-los/lokal. | storage.js · alle · fail-open (Prod faktisch datenlos) · Rot: nur bei Projektwechsel |
| `SUPABASE_SERVICE_ROLE_KEY` (auch `SUPABASE_SERVICE_KEY`, `SUPABASE_SECRET_KEY`) | P·S | Service-Role-Key (RLS-Bypass) — der reale DB-Zugang. | storage.js · alle · fail-open (Store inert) · Rot: bei Verdacht, siehe secret-rotation.md |
| `SUPABASE_JWT_SECRET` | O·S | Nur für den (stillgelegten) Tenant-JWT-Modus. Aktuell wirkungslos. → Abschnitt 9 | storage.js · — · wirkungslos · Rot: entfällt (veraltet) |
| `SUPABASE_ANON_KEY` | O·S | Anon-Key für authenticated-Requests (nur JWT-Modus). | storage.js · — · wirkungslos (JWT-Modus aus) · Rot: entfällt |
| `PILOT_SECRET` (auch `HELMUT_PILOT_SECRET`) | P·S | Geteilter Pilot-Zugangscode. **Fail-closed:** auf Vercel ohne diesen Wert kein Zugang. Rotation = Freigabepunkt FA-1 (früher F1; am 2026-07-15 ausgeführt, `/api/pilot/unlock` → HTTP 200 verifiziert). | server.js · Prod · fail-closed · Rot: FA-1 erledigt / sonst bei Verdacht |
| `HELMUT_ADMIN_SECRET` | O·S | Admin-/Debug-Bypass für einzelne Endpunkte; Default = `CRON_SECRET` (empfohlen: SEPARAT setzen, sonst gewährt das Cron-Secret Debug-Vollzugriff). | server.js · Prod · fail-closed (404 ohne Secret) · Rot: bei Verdacht |
| `CRON_SECRET` (GitHub-Secret: `HELMUT_CRON_SECRET`) | P·S | Bearer für alle `/api/cron/*`. **Fail-closed:** ohne → 503. **Doppelpflege:** identischer Wert in Vercel UND als GitHub-Secret `HELMUT_CRON_SECRET` (briefing-watchdog). | server.js · Prod+GitHub · fail-closed · Rot: beide Orte gleichzeitig |
| `OPENAI_API_KEY` | P*·S | OpenAI-Key. *Pflicht nur, wenn Azure NICHT gesetzt. Ohne KI: Regel-Fallbacks. | ai.js · Prod (+GitHub staff-backfill) · fail-open (Regelmotor) · Rot: Provider-Konsole |
| `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_KEY` | P*·S | Azure OpenAI (EU) — hat Vorrang vor OpenAI. Empfohlener Produktionspfad. | ai.js · Prod (+GitHub staff-backfill) · fail-open (Fallback OpenAI/Regeln) · Rot: Azure-Portal |
| `AZURE_OPENAI_DEPLOYMENT` | O | Deployment-Name. Default `gpt-5-mini`. | ai.js · Prod · Default greift · — |
| `AZURE_OPENAI_API_KEY` | O·S | Nur Alias-Fallback für `AZURE_OPENAI_KEY` in den Embedding-Werkzeugen (Sprint 22B/22C1); die Repo-Konvention bleibt `AZURE_OPENAI_KEY`. Nicht zusätzlich setzen. | embedding-backfill.js/embedding-testlauf.js · Cloud-Session · fail-closed (ohne Key kein Embedding-Lauf) · Rot: Azure-Portal |
| `HELMUT_EMBEDDING_DEPLOYMENT` | O·S | Azure-Deploymentname für semantische Embeddings (`text-embedding-3-small`, Sprint 22B/22C1). Nur für freigegebene Embedding-Läufe in der Cloud-Session nötig — **kein** Vercel-Runtime-Bedarf in 22C1 (die App liest die Shadow-Tabelle nicht). | embedding-backfill.js/embedding-testlauf.js · Cloud-Session · fail-closed (ohne Wert kein Lauf) · Rot: entfällt (kein Secret, nur Name) |
| `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT` (auch `HELMUT_VAPID_PUBLIC_KEY` / `HELMUT_VAPID_PRIVATE_KEY` / `HELMUT_VAPID_SUBJECT`) | O·S | Web-Push-Schlüssel. Ohne: Push deaktiviert (App läuft). Erzeugung: `scripts/generate-vapid-keys.js`. | push.js · Prod · fail-open (Push aus) · Rot: nur bei Kompromittierung (invalidiert ALLE Push-Abos) |
| `CALLMEBOT_PHONE` / `CALLMEBOT_APIKEY` | O·S | WhatsApp-Health-Report an den Betreiber. **Ohne → dieser Kanal wird STILL übersprungen** (siehe Zweitkanal `HELMUT_MONITORING_WEBHOOK_URL`). | server.js · Prod · fail-open (still!) · Rot: CallMeBot-Neuregistrierung |
| `DIP_API_KEY` | O·S | Bundestag-DIP-API (Drucksachen). Ohne: DIP-Feature inaktiv. | dip.js · Prod · fail-open (DIP aus) · Rot: bei Ablauf/Leak |
| `OPENAI_MODEL` | O | Modellname für den direkten OpenAI-Pfad. Default gpt-4.1. | ai.js · alle · Default greift · — |

## 2. Betriebsmodus / Auth (nicht geheim)

| Variable | P/O | Zweck / Default | Merkmale |
|---|---|---|---|
| `HELMUT_AUTH_MODE` | O | `accounts` = Konten-Login; sonst Legacy-Pilotgate. | server.js/auth.js · Prod · Default Legacy-Gate · — |
| `HELMUT_PROTECTED_TENANT_IDS` | O | Optionale zusätzliche Schutzliste (Komma-Liste von Mandats-IDs) für die Provisionierung; bestehende, nicht vom Werkzeug angelegte Mandanten sind auch ohne Wert datengetrieben geschützt. | provisioning.js · alle · Default leer (Marker-Schutz greift) · — |
| `HELMUT_TENANT_MODE` | O | Default `pilot`. | server.js · alle · Default greift · — |
| `HELMUT_TENANT_JWT_MODE` | O | **Stillgelegt** (tenantJwtModeEnabled()=false). Ohne Wirkung. → Abschnitt 9 | storage.js · — · wirkungslos · — |
| `HELMUT_PROFILE_DB_MODE` | O | Profile aus mandate_profiles statt Blob (Dual Write). Default aus. | storage.js · alle · Default aus · — |
| `HELMUT_PROFILE_DB_EXCLUSIVE` | O | Stufe E: Profile relational-only, saveProfile schreibt keinen helmut_store-Blob mehr. Setzt `HELMUT_PROFILE_DB_MODE=1` voraus. Default aus. Nur nach verifiziertem Backfill. Rollback = Flag leeren. | storage.js · alle · Default aus (Dual Write) · — |
| `HELMUT_V3_STORE` | O | V3-Relationstabellen nutzen. In Prod = 1. | storage.js · Prod=1 · ohne: V3 inert (fail-open) · — |
| `HELMUT_ADMIN_EMAIL` / `HELMUT_ADMIN_PASSWORD` / `HELMUT_ADMIN_NAME` / `HELMUT_ADMIN_RESET` | O·(S bei Passwort) | Erst-Admin-Seed (nach erstem Start entfernbar). | server.js · Prod (einmalig) · nur beim Seed relevant · nach Nutzung entfernen |
| `HELMUT_ALLOW_QUERY_SECRETS` | O | Query-Secret-Login erlauben. Default false. **In Prod ungesetzt lassen.** | server.js · alle · Default sicher (aus) · — |
| `HELMUT_SESSION_TTL_MS` | O | Session-Laufzeit. Default 30 Tage. | accounts.js · alle · Default greift · — |
| `HELMUT_INVITE_TOKEN_TTL_MS` | O | Gültigkeit des Einladungs-Links (Invite-Token, Umsetzungsnotiz §6). Default 7 Tage. | accounts.js · alle · Default greift · — |
| `HELMUT_RESET_TOKEN_TTL_MS` | O | Gültigkeit des Passwort-Reset-Links. Default 1 Stunde. | accounts.js · alle · Default greift · — |
| `HELMUT_RESET_ANTWORT_MS` | O | Antwortgitter des **anonymen** Passwort-Resets (`POST /api/auth/request-reset`) in Millisekunden. Default 500. Die Antwort verlässt den Server nur zu `t0 + n·Fenster`, damit bekannte und unbekannte Adressen zeitlich ununterscheidbar bleiben. Wert wird **hart auf 50…5000 ms geklemmt** — eine Fehlkonfiguration kann den Schutz weder abschalten noch den Endpunkt unbrauchbar machen; die eigentliche Maßnahme (Token/Mail/Audit außerhalb des Antwortpfads) wirkt unabhängig davon. In Production **nicht setzen**, der Default ist richtig. ([`../CURRENT_STATE.md`](../CURRENT_STATE.md), `lib/helmut/reset-timing.js`) | reset-timing.js/server.js · alle · Default greift · — |
| `HELMUT_PUBLIC_URL` | O | Basis-URL für öffentliche Zugangs-Links (`/passwort-setzen?token=…`). Default: aus Request-Headern (x-forwarded-host/-proto) abgeleitet. | server.js · alle · Default greift · — |
| `HELMUT_MAIL_FROM` | O | Absender für Invite-/Reset-Mails (**die einzige Absenderkonfiguration**, es gibt bewusst keine zweite). Default Platzhalter `Helmut <no-reply@…de>` — ohne gültige Domain und deshalb von **beiden** Transporten **ehrlich abgelehnt** (`mailpit-absender-ungueltig` bzw. `resend-absender-ungueltig`). Lokal eine reservierte Adresse setzen (`Helmut <noreply@helmut.test>`), für echten Versand eine Adresse der bei Resend **verifizierten** Domain. | invite-mail.js/mail-transport.js · alle · Versand aus (ehrlicher sent=false-Status) · — |
| `HELMUT_MAIL_REPLY_TO` | O | Antwortadresse (`Reply-To`) für Invite-/Reset-Mails, z. B. `Helmut Büro <buero@…>`. Leer = kein `Reply-To`. Ungültiger Wert → `resend-antwortadresse-ungueltig`, **kein** Versand (kein stilles Ignorieren). **Wirkt nur im Resend-Transport** — der Mailpit-Rumpf wurde bewusst nicht verändert. | mail-transport.js · alle · fail-closed (Versand aus) · — |
| `HELMUT_MAIL_TRANSPORT` | O | **Default AUS.** Die ausdrückliche Transportauswahl; genau zwei wirksame Werte (Groß-/Kleinschreibung und Leerraum egal, **kein** Präfix-Treffer), alles andere lässt den Versand aus (`mail-transport-unbekannt`). **`mailpit`** = lokales Testpostfach, **in Production und auf Vercel technisch gesperrt** (`NODE_ENV=production`, `VERCEL`, `VERCEL_ENV` — jeder nicht leere Wert, Sperre **vor** jeder Zielprüfung), gehört nie nach Vercel ([`lokale-mailtests-mailpit.md`](lokale-mailtests-mailpit.md)). **`resend`** = echter Versand über Resend, **vorbereitet, aber in Production NICHT aktiviert**; wirkt nur zusammen mit `HELMUT_RESEND_API_KEY` und gültigem `HELMUT_MAIL_FROM`. Aktivierung = Freigabeentscheidung ([`mailversand-resend.md`](mailversand-resend.md)). Ohne diese Variable ist das Verhalten byte-identisch zu vorher (`sent:false`, Grund `mail-versand-nicht-konfiguriert`, Kopierlink im Admin). | mail-transport.js/invite-mail.js · alle · fail-closed (Versand aus) · — |
| `HELMUT_MAILPIT_URL` | O | Basis-URL des lokalen Mailpit. Leer = `http://127.0.0.1:8025`. **Nur Loopback** (`127.0.0.1`, `localhost`, `::1`; exakter Hostvergleich, `0.0.0.0` abgelehnt), nur `http`/`https`, keine Zugangsdaten in der URL, kein Abfrageteil, keine Weiterleitungen (`redirect: "error"`), Zeitabbruch 3 s. Jede Abweichung → `sent:false` mit Grundcode, **nie** ein falscher Erfolgsstatus. | mail-transport.js · nur lokal · fail-closed · — |
| `HELMUT_RESEND_API_KEY` | O·S | **Secret.** Resend-API-Schlüssel für den echten Versand. Wird **ausschließlich** aus der Umgebung gelesen, steht in **keinem** Konfigurations- oder Antwortobjekt und geht nur in die `Authorization`-Kopfzeile. Ohne `HELMUT_MAIL_TRANSPORT=resend` **wirkungslos**; ohne Schlüssel → `resend-api-schluessel-fehlt`, kein Versand. Ziel-URL ist eine Code-Konstante (`https://api.resend.com/emails`) und bewusst **nicht** konfigurierbar. Empfohlen: Berechtigung `Sending access`, getrennte Schlüssel je Umgebung. In Production **noch nicht gesetzt.** Einrichtung/Rotation: [`mailversand-resend.md`](mailversand-resend.md) §3, [`secret-rotation.md`](secret-rotation.md) | mail-transport.js · Prod (nach Freigabe) · fail-closed (Versand aus) · Rot: bei Verdacht/Gerätewechsel |
| `HELMUT_CANONICAL_HOST` | O | Default `helmut-pilot.vercel.app`. | server.js · Prod · Default greift · — |
| `NODE_ENV` / `PORT` / `VERCEL` / `VERCEL_ENV` / `VERCEL_GIT_COMMIT_SHA` / `VERCEL_GIT_COMMIT_REF` | (Plattform) | Von Vercel/Node gesetzt. `VERCEL_GIT_COMMIT_SHA` speist die Asset-Versionierung (ASSET_VERSION). | Plattform · automatisch · — · — |

## 3. Kosten / KI-Budget

| Variable | P/O | Zweck / Default | Merkmale |
|---|---|---|---|
| `HELMUT_MAX_LLM_CALLS_PER_DAY` | P (empfohlen) | Globales Tageslimit. **Fail-closed:** NUR eine positive ganze Zahl setzt das Limit; fehlend, leer, `0`, negativ oder unparsebar → **Schutzlimit 50 Calls/Tag** (einmalige Log-Warnung). Einen „kein Limit"-Zustand gibt es seit dem Budget-Rollout NICHT mehr — wer mehr braucht, setzt explizit eine hohe Zahl. Production: 100. | storage.js (`llmDailyCallLimit`) · Prod · fail-closed (Schutzlimit 50) · — |
| `HELMUT_LLM_BUDGET_FAIL_CLOSED` | O | Bei Budget-Prüffehler KI verweigern statt erlauben. Default aus (Freigabepunkt FA-6, früher F6). Production: 1. | storage.js · Prod=1 · Default fail-open, mit Flag fail-closed · — |
| `HELMUT_LLM_RESERVE_UNDERSTANDING` | O | Reservierter Understanding-Mindestanteil am Tageslimit (Anzahl Calls). Nicht-Understanding-Pfade reservieren nur bis (Limit − Reserve). Leer/0 = aus; ungültig → 0. Production-Empfehlung: 30 (mit Limit 100, Runbook `llm-budget-reservierung.md`). | storage.js · Prod=30 empfohlen · Leer = Reserve aus (fail-open Richtung Verdrängung) · — |
| `HELMUT_TENANT_LLM_CAP` | O (Freigabepunkt) | Atomarer Kostendeckel JE MANDANT (Scope `tenant:<id>`), zusätzlich zum globalen Deckel. **Default AUS = verhaltensneutral** (reserveLlmCall byte-identisch, nur globaler Scope). 1/true/on = AN. Nutzt die bereits eingespielte Funktion `helmut_reserve_llm_call`; keine neue Migration. Freigabepflichtig, weil ein zu niedriges Limit echte Mandanten drosseln kann. | storage.js (`tenantLlmCapEnabled`/`reserveTenantScope`) · Prod=leer (aus) · Default aus · — |
| `HELMUT_MAX_LLM_CALLS_PER_TENANT_PER_DAY` | O | Uniformes Tageslimit (Anzahl KI-Calls) je Mandant, wenn `HELMUT_TENANT_LLM_CAP` AN ist und für den Mandanten kein Override existiert. Nur positive ganze Zahl; sonst **sicherer Fallback 40**. | storage.js (`tenantDailyCallLimit`) · Prod=leer · fail-safe (Fallback 40) · — |
| `HELMUT_TENANT_LLM_LIMITS` | O | Per-Mandant-Overrides als JSON-Karte (schlagen das uniforme Limit), z. B. `{"tenant-alpha":150,"tenant-beta":30}`. Ungültiges JSON → ignoriert (uniformer Default greift), einmalige Log-Warnung. Keine PII (nur Mandant-IDs + Zahlen). | storage.js (`tenantLlmLimitsMap`) · Prod=leer · Default kein Override · — |
| `HELMUT_UNDERSTANDING_LOCK` | O | Globaler Understanding-Lock gegen Doppelläufe. Default aus. | storage.js · Prod=1 empfohlen · Default aus · — |
| `HELMUT_FAILED_KO_RECOVERY` · `HELMUT_FAILED_KO_MAX_RETRIES` (Default 2) | O | **P1-4, FREIGABEPFLICHTIG, Default AUS.** Begrenzte automatische Wiederholung fehlgeschlagener Wissensobjekte im Understanding-Cron (failed → pending, bis `complete` oder nach `MAX_RETRIES` → `failed-final`, terminal). Schreibt Prod-KOs → nur nach Freigabe. Ohne Flag No-Op. Zähler im Auth-Store (keine Migration). | server.js/ko-recovery.js · Prod nach Freigabe · fail-open (No-Op) · — |
| `HELMUT_UNDERSTANDING_PRIORITY` | O | **P1-3, FREIGABEPFLICHTIG, Default AUS.** Schaltet die Understanding-Priorisierung scharf: bei Budgetdeckel werden die höchstpriorisierten Vorgänge zuerst verstanden (amtlich > hohe Relevanz > Frist > Wichtigkeit > regional > Grenzfall) statt in Ankunftsreihenfolge. Reine, KI-freie Umsortierung. Ohne Flag byte-identische Ankunftsreihenfolge. Verhaltensänderung → Freigabe. | understanding.js · Prod nach Freigabe · Default aus · — |
| `HELMUT_ATOMIC_LOCK` | O | **P0-4. Code-Default AUS — in Production seit 2026-07-16 AN.** Schaltet den atomaren, **fail-closed** Pipeline-Lock (Postgres `helmut_acquire_pipeline_lock`) statt des blob-basierten, fail-open Alt-Locks. Ohne Flag UND ohne Migration = bisheriges Verhalten byte-identisch. Mit BEIDEM: genau eine aktive Instanz, kein Doppel-KI-Call, sichere 05:30-Überlappungs-Koordination (mit `HELMUT_UNDERSTANDING_LOCK=on`). **KORREKTUR 2026-07-30 (Sprint OP-25):** hier stand bis dahin „Migration `20260719_pipeline_lock_atomic.sql` — NICHT auf Prod angewendet" und „Default fail-open" als Production-Zustand. **Beides war falsch** und widersprach `datenmotor-restliste.md` FT2-2 („live seit 2026-07-16 18:06 UTC, production-bewiesen") sowie `CLAUDE.md` §5 („offen: nur noch `20260720`"). Rein lesend in Production gegengeprüft: `helmut_acquire_pipeline_lock` und `helmut_release_pipeline_lock` **existieren**, `pipeline_locks.token` **existiert**, und die Zeilen des regulären 04:00-Crawls vom 2026-07-30 tragen einen **Token** — den schreibt ausschließlich der atomare Pfad. Belege: [`cron-fairness.md`](cron-fairness.md) §3a. | storage.js · **Prod AN** · Code-Default fail-open (Blob), in Prod fail-closed (atomar) · — |
| `HELMUT_LLM_PRICE_JSON` | O | Preis-Override für Kostenschätzung (USD je 1 Mio. Token, flacher Merge über die Code-Defaults). | storage.js · alle · Default-Preise greifen · — |
| `HELMUT_LLM_PRICE_SOURCE` | O | **Belegt die Preisbasis** (Punkt 17). Freitext, z. B. `"Providerrechnung 2026-07, Pos. 3"`. Solange nicht gesetzt, meldet `llmPriceProvenance()` `herkunft: "unbelegt-schaetzwert"` und der Betriebsbericht weist Kosten ausdrücklich als **berechnet**, nicht als Rechnungsbetrag aus. Ändert **keine** Zahl — nur die Herkunftsaussage. | storage.js · alle · Kosten gelten als unbelegter Schätzwert · — |
| `HELMUT_LLM_PRICE_ASOF` | O | Stand der Preisbasis (Datum, z. B. `2026-07-01`). Nur Anzeige, wirkt sich auf keine Berechnung aus. | storage.js · alle · kein Stand ausgewiesen · — |
| `HELMUT_UNDERSTANDING_MODEL` / `HELMUT_TEXT_MODEL` | O | Modell-Override (nur OpenAI-Pfad). Default gpt-5-mini. | ai.js · alle · Default greift · — |
| `HELMUT_OFFICE_DAILY_LIMIT` | O | Büro-Outputs/Tag (V3-Engine). Default 10. | office.js · alle · Default greift · — |
| `HELMUT_MAX_DAILY_INPUTS` | O | Tagesinputs/Mandat. Default 3. | server.js · alle · Default greift · — |

## 4. Quellen / Crawl / PARDOK

| Variable | P/O | Zweck / Default | Merkmale |
|---|---|---|---|
| `HELMUT_SOURCE_MODE` | P | off/shadow/**on** (Cutover aktiv). Via helmut-flags.json/Env (dynamisch gelesen über `flagValue`). | flags.js/source-mode.js · Prod=on · Default off (kein Crawl-Cutover!) · — |
| `HELMUT_UNDERSTANDING_GATE` / `HELMUT_PARDOK_DISPATCH` | O | Gate/Dispatch off/shadow/on. Beide shadow. Dynamisch über `flagValue`/helmut-flags.json. | flags.js · alle · Default off · — |
| `HELMUT_LANDESMODULE` | O | **Freigabe der Landesmodule je Land, FREIGABEPFLICHTIG, Default LEER = alle gesperrt.** Kommagetrennte Länderliste (heute nur `berlin`/`brandenburg`), z. B. `berlin`. Nur ausdrücklich genannte Länder laufen; unbekannte Werte wirken nicht und es gibt bewusst KEIN Sammel-Schlüsselwort (`alle`/`*` sind wirkungslos, fail-closed). Öffnet ausschließlich das Crawl-Gate — Paketstatus (`prepared`), Wegstatus (`manual`) und die Profil-Referenzzählung bleiben eigenständige Riegel. Leeren = sofortiger Rollback ohne DB-Zugriff. Runbook: `betrieb/berlin-aktivierung.md`. | flags.js/source-mode.js · Prod nach Freigabe · Default leer (alle Landesmodule gesperrt) · — |
| `HELMUT_SOURCE_BLOCKLIST` | O | Kommagetrennte Domains, die als Quelle GESPERRT werden — Betreiber-Schalter, wirkt sofort ohne Deploy (dynamisch gelesen: `envList("HELMUT_SOURCE_BLOCKLIST")`). Leer = keine Zusatzsperren. | sourceSafety.js · Prod (Notfallschalter) · Leer = aus · — |
| `HELMUT_SOURCE_ALLOWLIST` | O | Kommagetrennte Domains, die ZUSÄTZLICH als vertrauenswürdige Quelle erlaubt werden — Betreiber-Schalter ohne Deploy (`envList("HELMUT_SOURCE_ALLOWLIST")`). Leer = nur eingebaute Liste. | sourceSafety.js · Prod (bei Bedarf) · Leer = aus · — |
| `CRAWLER_TIMEOUT_MS` (Default 7000) · `CRAWLER_CONCURRENCY` | O | Crawl-Netzverhalten. | crawler.js · alle · Default greift · — |
| `HELMUT_GOOGLE_HARDENING` | O | **Google-News-Härtung (Sprint 2026-07), Default AN.** Kill-Switch: `off`/`0`/`false` stellt das Alt-Verhalten (ein Pool, keine Google-Drossel) ohne Deploy wieder her. | google-news-hardening.js · Prod · Default an · — |
| `HELMUT_GOOGLE_CONCURRENCY` (Default 5) · `HELMUT_GOOGLE_MIN_SPACING_MS` (Default 200) | O | Google-News-Gate: max. gleichzeitige Google-Quellenabrufe + Mindestabstand zwischen Google-Quellenstarts (ms). Direkte/amtliche Quellen sind NIE betroffen. Ungültige Werte → konservativer Default (fail-closed). | google-news-hardening.js · alle · Default greift · — |
| `HELMUT_GOOGLE_RETRY_MAX` (Default 2) · `HELMUT_GOOGLE_RETRY_BASE_MS` (Default 1000) · `HELMUT_GOOGLE_RETRY_CAP_MS` (Default 8000) · `HELMUT_GOOGLE_RETRY_AFTER_CAP_MS` (Default 15000) | O | Retry-Regeln für Google-Feeds: max. Wiederholungen bei 429/5xx (exponentieller Backoff + Jitter, Retry-After-Header respektiert und gedeckelt). Timeouts werden bewusst NICHT wiederholt. | google-news-hardening.js/crawler.js · alle · Default greift · — |
| `HELMUT_GOOGLE_BREAKER_MIN_OBSERVATIONS` (Default 10) · `HELMUT_GOOGLE_BREAKER_FAILURE_RATIO` (Default 0.6) | O | Circuit Breaker je Lauf: ab N beobachteten Google-VERSUCHEN mit ≥ Ratio Drossel-Fehlern (429/Timeout/5xx) enden restliche Google-Abrufe sofort (`circuit-open`). Fehlversuche zählen je Versuch (schnelle Öffnung im Sturm). | google-news-hardening.js · alle · Default greift · — |
| `HELMUT_GOOGLE_RETRY_BUDGET` (Default 12) · `HELMUT_GOOGLE_BREAKER_MEMORY_MS` (Default 600000) | O | Harte Retry-Obergrenze JE LAUF (Backoff-Schlafzeiten halten Gate-Slots — das Budget schützt das Serverless-Zeitlimit bei Teil-Drosselung) und Breaker-Gedächtnis: nach einer Breaker-Öffnung starten Folge-Läufe im selben Prozess (sequenzieller Mandanten-Loop) direkt fail-fast. 0 = aus. | google-news-hardening.js/scheduler.js · alle · Default greift · — |
| `HELMUT_GOOGLE_COOLDOWN_MS` (Default 3600000) · `HELMUT_FULL_CRAWL_MIN_SPACING_MS` (Default 1800000) | O | Cooldown: nach stark degradiertem Lauf fährt der nächste Vollcrawl Google reduziert (nur Primär-Feed, keine URL-Auflösung); ein Vollcrawl < Mindestabstand nach dem letzten überspringt den Google-Anteil ganz (Schutz vor eng aufeinanderfolgenden manuellen Vollcrawls). | google-news-hardening.js/scheduler.js · alle · Default greift · — |
| `HELMUT_RUNSTATE_PARTIAL_RATIO` (Default 0.1) · `HELMUT_RUNSTATE_HEAVY_RATIO` (Default 0.5) | O | Schwellen der Crawl-Lauf-Klassifikation (teilweise-/stark-degradiert). Empfehlung, keine endgültige Produktentscheidung — siehe `docs/betrieb/google_news_haertung.md`. | crawl-run-state.js · alle · Default greift · — |
| `HELMUT_RUNSTATE_MIN_ATTEMPTED` (Default 10) | O | Mindestzahl tatsächlich VERSUCHTER Abrufwege, ab der eine Fehlerquote als Lauf-Bewertung gilt. Greift nur bei bewusst reduzierten Läufen (übersprungene Wege > 0): aus einem winzigen Restanteil darf keine Degradation extrapoliert werden (Incident 2026-07-25). | crawl-run-state.js · alle · Default greift · — |
| `HELMUT_SHARED_PATH_DEDUP` (Default on) · `HELMUT_SHARED_PATH_WINDOW_MS` (Default 900000) | O | Geteilte Google-News-Abrufwege werden je Cron-Durchlauf nur EINMAL geholt (alle Mandate laufen sequenziell im selben Prozess hinter derselben Egress-IP; ~138 von 144 Wegen sind mandantenunabhängig identisch). Verhindert die selbst verursachte Drosselung, die Folge-Mandate als „141/144 Fehler" erscheinen ließ (Incident 2026-07-25). Kill-Switch: `off` stellt das Alt-Verhalten her. | google-news-hardening.js/crawler.js/scheduler.js · alle · Default greift · — |
| `HELMUT_WATCHDOG_LAGE_FRESH_MS` (Default 93600000 = 26 h) | O | Frischeschwelle der Watchdog-Lage-Achse, am CRON-TAKT gemessen (Lage-Cron `0 10 * * *` = 1×/Tag, Health-Report `0 6 * * *`). Die In-App-Schwelle `HELMUT_MAX_LAGE_CHECK_AGE_MS` (4 h) bleibt unverändert. Vorher erzeugte die 4-h-Schwelle gegen den 24-h-Takt jeden Morgen den Fehlalarm „Lage-Check veraltet" (Incident 2026-07-25). | server.js/watchdog-state.js · alle · Default greift · — |
| `HELMUT_CRAWL_MAX_RESPONSE_BYTES` | O | Standard-Crawl-Bytelimit. Default 10 MiB. | crawler.js · alle · Default greift · — |
| `HELMUT_PARDOK_MAX_RESPONSE_BYTES` (Default 64 MiB) · `HELMUT_PARDOK_MAX_RECORDS` (Default 800) | O | PARDOK-Streaming-Budget (Audit-Fix Sprint 7). | pardok-parser.js · alle · Default greift · — |
| `HELMUT_SOURCE_TARGET` / `HELMUT_SOURCE_CURATION` | O | Katalog-Kuratierung (Alt-Katalog-Pfad). | crawler.js · alle · Default greift · — |
| `HELMUT_GOOGLE_NEWS_MAX_ITEMS` · `HELMUT_DIRECT_RSS_MAX_ITEMS` · `HELMUT_PERSON_NEWS_MAX_ITEMS` · `HELMUT_PROFILE_NEWS_MAX_ITEMS` · `HELMUT_TOPIC_RADAR_MAX_ITEMS` | O | Item-Caps je Quellart. | crawler.js · alle · Default greift · — |
| `HELMUT_CRAWL_MAX_CANDIDATES` · `HELMUT_CRAWL_LAZY_BUDGET_MS` · `HELMUT_CRAWL_UNDERSTAND_BUDGET_MS` · `HELMUT_UNDERSTAND_BUDGET_MS` · `HELMUT_LAGE_UNDERSTAND_BUDGET_MS` | O | Zeit-/Mengenbudgets der Pipeline. | scheduler.js/understanding.js · alle · Default greift · — |
| `HELMUT_VERSTEHEN_WIEDERAUFNAHME_MAX` (Default 25) | O | **Wiederaufnahme §30 (Sprint 2026-08-22).** Obergrenze der je Nachhollauf zusätzlich aufgenommenen, per kanonischem Betreiberweg **ausdrücklich `erneut` freigegebenen** Vorgänge (`zustand=offen` + `letzter_grund=erneut-freigegeben`). Begrenzt nur, wie viele Freigaben EIN Lauf aufnimmt — nicht, wie oft ein Vorgang wiederholt wird (das regeln unverändert die CAS-Zähler und der Wiederaufnahmedeckel). Hart auf 200 geklemmt. | understanding.js/storage.js · alle · Default greift · — |
| `HELMUT_KI_TIMEOUT_MS` (Default 20000) · `HELMUT_VERSTEHEN_RESTZEIT_RESERVE_MS` (Default-Kernreserve = KI-Timeout + 2× Supabase-Timeout + 5000 = 45 s; vor dem Modellstart gilt Kernreserve + 1 Supabase-Timeout = 55 s) | O | **Restzeitwache §29 (Reparatursprint 2026-08-20 inkl. Review-Korrektur).** KI-HTTP-Timeout (vorher Literal in ai.js) und die zentrale Sicherheitsreserve, unter der kein neuer Verstehens-Modellaufruf mehr beginnt (Modellantwort + Schreibrecht + Speicherung + Abschluss; vor dem Modellstart zusätzlich der `modellstart`-RPC). Defaults = bisheriges Verhalten; eine Erhöhung des KI-Timeouts hebt beide Reserven automatisch mit an. | ai.js/verstehen-restzeit.js · alle · Default greift · — |
| `HELMUT_CRAWL_ABSCHLUSS_RESERVE_MS` (Default 10000) · `HELMUT_CRAWL_VORMERK_RESERVE_MS` (Default 30000) · `HELMUT_PENDING_BULK_CHUNK` (Default 200) | O | **OP-25 K4/K8 (Korrektursprint 2026-08-05).** Zeitvertrag der globalen Phase: `…ABSCHLUSS_RESERVE_MS` reserviert das Ende des Phasenbudgets für Bilanz/Telemetrie/Versiegelung (vorher implizit 5 s — zu knapp, Befund +313 ms); `…VORMERK_RESERVE_MS` reserviert davor die Zeit für die gebündelte Vormerkung des gesamten Rückstands; die fachlichen Schleifen enden vor beiden Reserven. `…PENDING_BULK_CHUNK`: Chunk-Größe der Bulk-Vormerkung (`savePendingKnowledgeObjectsBulk`, F-RT-Muster). Beide Reserven heben **kein** Budget an. Kanonisch: [`vorgangskontext.md`](vorgangskontext.md) §7.7.6/§7.7.7. | scheduler.js/storage.js · alle · Default greift · — |
| `HELMUT_CRAWL_GESAMTBUDGET_MS` | O | **Gesamt-Zeitbudget eines `runSourceCrawl`-Laufs, Default 240000 (80 % von `maxDuration` 300 s).** Die Einzelbudgets oben werden daraus als `min(eigenes Budget, Restzeit)` abgeleitet — vorher begrenzte nichts die **Summe**, und der Lauf riss das Funktionslimit, bevor er seine Telemetrie schreiben konnte (Betriebsbefund 2026-07-27). Kleiner setzen = früher abbrechen, größer setzen = Timeout-Risiko. | scheduler.js · alle · Default greift · — |
| `HELMUT_CRON_FAIRNESS` | O | **Faire Mandantenreihenfolge der Mehrmandanten-Crons (OP-25), Default AN.** Die Crons verarbeiteten die aktiven Mandate alphabetisch gegen ein hartes Zeitbudget — bei knapper Laufzeit fiel deshalb immer dasselbe Mandat aus (belegt 2026-07-24 und 2026-07-29). Jetzt: ältester letzter Versuch zuerst, persistent in einer eigenen `helmut_store`-Zeile. **`off` = Rückweg auf die alte alphabetische Reihenfolge ohne Codeänderung** (dann auch kein Zustands-Schreibzugriff). Kanonisch: [`cron-fairness.md`](cron-fairness.md). | cron-fairness.js/server.js · alle · Default greift (AN) · — |
| `HELMUT_CRON_FAIRNESS_STALE_MS` (Default 1800000 = 30 min) · `HELMUT_CRON_TENANT_RESERVE_MS` (Default 15000) | O | Fairness-Feinstellung. `…STALE_MS`: ab wann ein als `laufend` vermerkter Versuch als veraltet gilt und das Mandat kontrolliert erneut zugelassen wird (muss **größer** als `maxDuration` 300 s und als die Crawl-Lock-TTL 15 min bleiben). `…RESERVE_MS`: Mindest-Restlaufzeit, bevor ein **weiteres** Mandat begonnen wird — verhindert, dass ein Mandat als „versucht" gilt, ohne echte Arbeit geleistet zu haben. Beide heben **kein** Zeitbudget an. | cron-fairness.js · alle · Default greift · — |
| `HELMUT_CRON_GLOBALPHASE` | O | **OP-25 K1 — Trennung von globaler Erfassung und mandatsbezogener Projektion. DEFAULT AUS, in Production NICHT gesetzt.** Nur `on`/`true`/`1`/`an` schalten ein; jeder andere Wert (auch leer, auch ein Tippfehler) bedeutet AUS (fail closed). Ohne gesetzten Wert laufen `/api/cron/crawl` und `/api/cron/pipeline` **exakt** wie bisher. Mit gesetztem Wert: Quellen einmal je Lauf abrufen und verstehen, danach je Mandat nur Matching + Entscheidungen. **Nicht** über `helmut-flags.json` setzbar — die Aktivierung ist ausschließlich eine Vercel-Env-Entscheidung des Betreibers. Rückweg: Wert auf `off` oder Variable löschen, Redeploy. Kanonisch: [`cron-globalphase.md`](cron-globalphase.md). | cron-globalphase.js/scheduler.js/server.js · alle · **nicht gesetzt (AUS)** · — |
| `HELMUT_CRON_GLOBALABRUF` | O | **OP-25 K2.1 — globaler Abruf mit KONTEXTGEBUNDENER Vorgangsbildung.** **Code-Default: AUS** (ohne gesetzten Wert läuft ausschließlich der Altpfad). **Aktueller Production-Zustand (Stand 2026-08-08): `on` — Betreiberbestätigung** (ausdrücklich bestätigt im Abschlussreview zu PR #233 am 2026-08-08; keine Vercel-API-Einsicht, keine Environment-Änderung durch diese Sitzung). *Korrektur einer veralteten Statusangabe:* hier stand bis zum 2026-08-08 `off` mit Stand 2026-08-06. Das war der Zustand zum Zeitpunkt des Redeploys `dpl_3y5n…` (07:50:22Z, [`production_beweisprotokoll.md`](production_beweisprotokoll.md) §9 — diese Belegzeile bleibt als historischer Nachweis unverändert stehen); der Betreiber hat das Flag am selben Tag gegen ~08:15 UTC für das dritte OP-25-Nachweisfenster auf `on` gesetzt ([`CURRENT_STATE.md`](../CURRENT_STATE.md) §5) und es ist seither `on`. Der Widerspruch zwischen beiden Dokumenten ist damit aufgelöst (im Zuge der OP-25-Betreiberschritte; aus Claude-Sitzungen ist der Wert wegen der Egress-Sperre auf `api.vercel.com` **nicht** über die Vercel-API lesbar — die Bestätigung ist eine Betreiber-Sichtprüfung, keine API-Einsicht). Die Variable war vom 2026-08-03, 13:15:11 UTC bis zum Rückbau auf `on`; der erste reguläre Wirkungslauf ist am Kapazitätsnachweis gescheitert (globale Phase 267,12 s bei Budget 221,674 s, **0 von 6** Mandaten, Befunde **F-RT**/**F-CL**), das Rückbau-Deployment war `READY`. Ursache, Reparatur und die drei offenen Entscheidungen: [`vorgangskontext.md`](vorgangskontext.md) **§7.6**. Eine Wiederaktivierung setzt Review und Merge der Reparatur voraus und startet ein **vollständig neues** Beobachtungsfenster. *Die folgenden Angaben zum Zeitraum der Aktivierung bleiben als Beleg stehen und beschreiben **nicht** den heutigen Zustand.* Nur `on`/`true`/`1`/`an` schalten ein; jeder andere Wert (auch leer, auch ein Tippfehler) bedeutet AUS (fail closed). Unterschied zu `HELMUT_CRON_GLOBALPHASE`: der Abruf ist derselbe (eine Vereinigungsmenge, jeder Abrufweg höchstens einmal), aber die **Bündelung** läuft je **Sichtbarkeitsmenge** statt global — damit kann keine Quelle eines anderen Mandats die Vorgangsidentität verändern (Befund K1-1, [`cron-globalphase.md`](cron-globalphase.md) §8a). **Sind beide Flaggen gesetzt, läuft der ALTPFAD** (fail closed bei Widerspruch, laut protokolliert). **Nicht** über `helmut-flags.json` setzbar — die Aktivierung ist ausschließlich eine Vercel-Env-Entscheidung des Betreibers. Rückweg: Wert auf `off` oder Variable löschen, Redeploy. Kanonisch: [`vorgangskontext.md`](vorgangskontext.md). **Seit 2026-08-03, 13:15:11 UTC / 15:15:11 Berlin in Production auf `on`** (Betreiberaktion über die Vercel-Oberfläche, ausschließlich `Production`; Preview und Development unverändert; `Sensitive` bewusst aus, weil der Wert kein Geheimnis ist). Redeploy `dpl_J4g3k4QPUEaKAad3pB83ByGcvUkn` auf Commit `ded0e24`, `READY`. **Der reguläre Production-Kapazitätsnachweis über ≥ 24 h ist noch offen** ([`vorgangskontext.md`](vorgangskontext.md) §7.4); das dabei maßgebliche Kontextkriterium prüft **Verträge, keine Zahlengrenze** (Partition, Kontextgrenze, vollständig ausgewiesene unbekannte Kontexte, keine `kontextvertrag`-Fehler) und wird an der Zeile `[globalphase/kontext]` abgelesen — kanonisch in §7.5 derselben Datei, inklusive der Klarstellung, dass `1 ≤ kontexte ≤ 2n + 1` **nur** eine Beobachtung der Simulationsprofilwelt ist. | vorgangskontext.js/cron-globalphase.js/scheduler.js/server.js · **Production: `on` (Betreiberbestätigung 2026-08-08)**, Preview/Development nicht gesetzt · — |
| `HELMUT_CRON_PROJEKTION_MS` (Default 8000) · `HELMUT_GLOBALPHASE_ABRUF_STUFE` (Default 20) | O | Feinstellung der globalen Phase, nur wirksam bei gesetztem `HELMUT_CRON_GLOBALPHASE` **oder** `HELMUT_CRON_GLOBALABRUF`. `…PROJEKTION_MS`: Reserve **je Mandat**, die vom Laufbudget für die Mandatsphase zurückgehalten wird (Production gemessen: ein Matchinglauf ≈ 1,05 s). `…ABRUF_STUFE`: Zahl der Abrufwege je Stufe — zwischen zwei Stufen wird die Restzeit geprüft. **Das ist ein START-Gatter, kein Stopp-Gatter:** eine begonnene Stufe läuft ungebremst zu Ende, und `CRAWLER_TIMEOUT_MS` begrenzt sie nicht (es ist ein Timeout je EINZELNER Anfrage; eine Google-Quelle löst offline gemessen 37–98 Anfragen aus, Production einzelne Quellen bis 41 892 ms bei 7 000 ms Limit und `retry_count = 0`). **Den Wert zu SENKEN hilft nicht, es schadet:** eine Stufe wartet auf ihre langsamste Quelle, also ist die Summe der Stufenmaxima eine Untergrenze der Abrufdauer und steigt beim Verfeinern monoton — an den 181 gemessenen Quellendauern des 2026-08-03-Laufs 20 → **71,3 s**, 10 → 90,2 s, 5 → **153,0 s** —, und weil `plan.quellen` unabhängig vom Quellentyp geschnitten wird, starten auch die **direkten/amtlichen** Quellen später (späteste Startuntergrenze 9,85 s → 31,53 s). Belege: `scripts/quellen-mehrfachabruf-test.js`, [`vorgangskontext.md`](vorgangskontext.md) §7.6.1. **Beide heben kein Zeitbudget an**; die globale Phase erhält höchstens `HELMUT_CRAWL_GESAMTBUDGET_MS` und nie weniger als 50 % der Restzeit. | cron-globalphase.js/scheduler.js · alle · Default greift · — |
| `HELMUT_SCALABLE_PIPELINE` | O | **OP-30 — skalierbarer Pfad über die dauerhafte Arbeitswarteschlange.** **Code-Default: AUS**; ohne gesetzten Wert wird von `lib/helmut/scalable-pipeline.js` nichts betreten und `cronSchwererPfad` verhält sich funktional unverändert (Nachweis: `scripts/scalable-pipeline-flag-test.js` §1–§3). Nur `on`/`true`/`1`/`an` schalten ein; jeder andere Wert — auch leer, auch ein Tippfehler — bedeutet AUS (fail closed). **Bei `on` läuft AUSSCHLIESSLICH die Warteschlange**, keiner der bisherigen Zweige (`alt`/`globalphase`/`kontext`) — der Einsprung ist ein `return` vor der bisherigen Pfadwahl, es gibt also keine Konstellation mit doppelter externer Arbeit. **Nicht** über `helmut-flags.json` setzbar (bewusst nicht in der Allowlist, gleiche Begründung wie `HELMUT_CRON_GLOBALABRUF`): ein Flag, das externen Abruf scharf schaltet, darf nicht per Datei-Commit aktivierbar sein. **Voraussetzung:** Migration `20260808_scalable_job_queue.sql` muss angewendet sein — sonst meldet die Warteschlange ehrlich `verfuegbar:false / migration-fehlt` und der Lauf gilt als nicht ok (kein stiller Erfolg). Rückweg: Wert auf `off` oder Variable löschen, Redeploy — der bisherige Pfad läuft danach unverändert weiter; die Tabelle darf gefahrlos stehen bleiben. **Production-Zustand (Stand 2026-08-24): `on`, gesetzt am 23.08.2026 16:47 UTC** ([`op30-aktivierung-5-mandate.md`](op30-aktivierung-5-mandate.md) §30.7). Die frühere Angabe „nicht gesetzt, in keiner Umgebung aktiviert" ist **überholt**. Kanonisch: [`skalierungsgrundlage-1000.md`](skalierungsgrundlage-1000.md). | scalable-pipeline.js/server.js · **Production: `on` seit 23.08.2026** · — · — |
| `HELMUT_SCALABLE_PIPELINE_SEIT` (ohne Vorgabe) | O | **OP-30 — erklaerter Aktivierungszeitpunkt des skalierbaren Pfads (Warteschlangenwache V2, Runbook §26.4).** ISO-8601-Zeitstempel (z. B. `2026-08-20T18:15:00Z`), den der Betreiber **zusammen mit** `HELMUT_SCALABLE_PIPELINE=on` setzt. Die Betriebsgrenze der Wache misst dann die **effektive Wartezeit** `min(Wartezeit, jetzt − SEIT)`: ein waehrend der Abschaltung entstandener, nachweislich nie ausfuehrbarer Auftrag wird nicht allein wegen seines Erstellungsdatums `kritisch` (dritte Falle, Runbook §17.7(d)) — sobald er ausfuehrbar ist, laeuft die 18-h-/24-h-Frist unverkuerzt. **Fail closed:** ohne die Variable gilt die rohe Wartezeit (Fehlalarm zulaessig, falsches Gruen nie); ein unlesbarer Wert wird als Befund benannt und ignoriert; ein Zeitpunkt in der Zukunft ist ein Widerspruch und macht den Zustand `unbekannt`. Die Klemmung ist immer als Befund `wartezeit-ab-aktivierung:…` sichtbar, `rohesAlterS` bleibt daneben stehen. Beim Zuruecknehmen des Flags mit entfernen. Nachweis: `scripts/warteschlangenwache-vertrag-test.js` §4/§7/§10. | scalable-pipeline.js · **nirgends gesetzt** · rohe Wartezeit gilt · nur wirksam mit `HELMUT_SCALABLE_PIPELINE` |
| `HELMUT_NARRATIV_QUEUE` (Default **AUS**) | O | **OP-30/E1 — Lage-Narrativ als fünfter Auftragstyp `tenant_narrative` über die Warteschlange** (Sprint 2026-08-09/2). **Wirksam NUR zusammen mit `HELMUT_SCALABLE_PIPELINE`** — einzeln gesetzt ist es vollständig wirkungslos (kein Planer, kein Worker). Fail closed: nur `on`/`true`/`1`/`an` schalten ein. Bei beiden Flags: der Scheduler plant je aktivem Mandat und 24-h-Fenster **einen** `tenant_narrative`-Auftrag (Morgenkorridor 05:45–08:00 UTC), der Cron `lage-briefing` wird zum reinen Narrativ-Worker (`return`-Zweig — strukturell kein Doppelpfad zur bisherigen Direktschleife), der Handler ruft die **unveränderte** `lage.buildLageBriefing` auf. **Voraussetzung:** Migration `20260809_jobqueue_narrativ.sql` (**nicht angewendet**) — sonst lehnt `helmut_enqueue_job` den Typ ab (`helmut_jobs_type_chk`, ehrlich sichtbar als `nichtEingereiht`). Rückweg: Wert auf `off`/löschen + Redeploy — die Direktschleife übernimmt sofort wieder; Restauträge des laufenden Fensters räumt der Worker ohne Modellaufruf ab. **Warum ein eigenes Flag:** der Aktivierungsplan schaltet die Queue in Stufe 2, E1 erst ab Stufe 5 — und der Narrativ-Rückweg muss ohne Abschaltung der gesamten Warteschlange möglich sein. Nachweis: `scripts/tenant-narrativ-test.js` (90) + `scripts/flagmatrix-op30-test.js` §5b. Kanonisch: [`lage-narrativ-warteschlange-2026-08-09.md`](lage-narrativ-warteschlange-2026-08-09.md). | scalable-pipeline.js/source-demand.js/server.js · **nirgends gesetzt** · Default AUS · nur wirksam mit `HELMUT_SCALABLE_PIPELINE` |
| `HELMUT_JOB_DISPATCH_MODE` (Default **off**) | O | **OP-30-Zielarchitektur — Modus des austauschbaren Transports** (Sprint 2026-08-13/3). `off` = kein Outbox-Schreiben, kein Versand (byte-identisches Bestandsverhalten) · `shadow` = Auftrag+Versandabsicht+Versandplanung werden bewiesen, nichts verlässt den Prozess · `queue` = Versand über `HELMUT_JOB_TRANSPORT`. **Jeder andere Wert fällt geschlossen auf `off`** und wird als Widerspruch benannt (`job-dispatch.waehleAntrieb`). Wirksam nur mit `HELMUT_SCALABLE_PIPELINE`; Voraussetzung Migration `20260813090000_jobqueue_outbox.sql` (**seit 15.08.2026 angewendet** — die frühere Angabe „nicht angewendet" ist überholt). Es gibt genau EINEN primären Antrieb (`bestand`/`cron-queue`/`ereignis`); die Verbraucher-Route `/api/cron/worker-weck` arbeitet ausschließlich im Antrieb `ereignis` (sonst 409). Rückweg: Wert löschen/`off` — offene Absichten bleiben folgenlos liegen, kein Auftrag geht verloren. **Aktivierungsvorlauf (Härtungssprint 2026-08-24):** `job-dispatch.aktivierungsVorpruefung` meldet maschinenlesbar, ob der Ereignis-Antrieb **tatsächlich** bereit ist (angeforderter Modus · wirksamer Modus · Antrieb · Transport · Verfügbarkeit · Grund · `bereit`); `/api/ops/jobqueue` gibt sie als Feld `ereignisbetrieb` aus. Ein Queue-Modus **ohne** `HELMUT_SCALABLE_PIPELINE` versendet seit demselben Sprint nichts mehr und vergibt keine Absicht (`uebersprungen: antrieb-bestand`) — vorher verbrannte er Versuche für Signale, die die Route ohnehin mit 409 abwies. Nachweis: `scripts/jobdispatch-vertrag-test.js` (91) + `scripts/selbstweck-ende-zu-ende-test.js` (31) + `scripts/jobqueue-outbox-datenbank-test.js` (37). Kanonisch: [`op30-zielarchitektur-2026-08-13.md`](op30-zielarchitektur-2026-08-13.md). | job-dispatch.js/server.js · **Production: `shadow` (Stand 2026-08-24)** · — · nur wirksam mit `HELMUT_SCALABLE_PIPELINE` |
| `HELMUT_JOB_TRANSPORT` (Default **`sqs`**) · `HELMUT_WORKER_WAKE_URL` · `HELMUT_WAKE_TIMEOUT_MS` (Default 3000) · `HELMUT_QUEUE_TOPIC` (Default `helmut-auftraege`) | O | **OP-30-Zielarchitektur — Transportwahl und Selbstweck-Konfiguration**, nur wirksam bei `HELMUT_JOB_DISPATCH_MODE=queue`. `selbstweck`: GEBÜNDELTER Weckruf (Türklingel: ein POST je Versandkontext) des Zwei-Felder-Payloads `{jobId, schemaVersion}`, autorisiert mit dem bestehenden `CRON_SECRET` (Bearer). **`HELMUT_WORKER_WAKE_URL` unterliegt dem Weckziel-Riegel (Sicherheitskorrektur 2026-08-14, Belegdatei §17.2):** nur HTTPS, exakt `/api/cron/worker-weck`, kein Userinfo/Query/Fragment/Port, und der Host muss einem von der Plattform gesetzten Deployment-Host entsprechen (`VERCEL_PROJECT_PRODUCTION_URL`/`VERCEL_URL`/`VERCEL_BRANCH_URL`) — jede Abweichung: `transport-nicht-verfuegbar`, das Secret verlässt den Prozess nie. `HELMUT_WAKE_TIMEOUT_MS`: maximale Antwortwartezeit des Senders; ein Timeout NACH dem Absenden gilt als **unbestätigt** (nichts wird verbucht; Vergabe-Backoff + Abgleich tragen), nie als definitiver Fehlversuch. `vercel-queues`: Adapter nach offizieller SDK-Signatur — **fail closed ohne installiertes SDK; Aktivierung ist eine kostenpflichtige Gründerentscheidung** (Public Beta, keine EU-Residenzzusage im Failover). Unbekannte Transportnamen: fail closed, kein stiller Rückfall. Mehr als die zwei Payload-Felder kann strukturell nicht übertragen werden (`pruefeTransportPayload` beim Versand UND als Eingangsprüfung der Route + Outbox ohne Inhaltsspalte). **Für den Selbstweck-Weg sind `HELMUT_JOB_TRANSPORT=selbstweck` und `HELMUT_WORKER_WAKE_URL` zwei der fünf nötigen Werte — vollständige Liste und Vorprüfung: §7a.** | job-dispatch.js · **nirgends gesetzt** · Defaults greifen · nur wirksam bei Dispatch `queue` |
| `HELMUT_SQS_QUEUE_URL` · `AWS_REGION` (Default `eu-central-1`) | O | **OP-30 verwalteter Transport (Amazon SQS).** Die Queue-URL unterliegt einem harten Riegel: nur HTTPS, nur Host `sqs.<region>.amazonaws.com`, kein Userinfo/Query/Fragment — und **nur die Region `eu-central-1`** (Datenresidenz ist im Code hart, nicht konfigurierbar). Jede Abweichung: Transport geschlossen nicht verfügbar, kein SDK-Aufruf. Über die Grenze gehen ausschließlich `{jobId, schemaVersion}`. **Es existiert keine AWS-Ressource** — Anlegen ist eine kostenpflichtige Gründerentscheidung (Belegdatei §19). | job-dispatch.js · **nirgends gesetzt** · nur wirksam bei Dispatch `queue` und Transport `sqs` |
| `HELMUT_SUPABASE_URL_PARAMETER` · `HELMUT_SUPABASE_KEY_PARAMETER` | O | **OP-30 sicherer Startweg der Lambda-Funktionen (Korrekturlauf 2026-08-14/3).** Sie tragen **NAMEN von AWS-SSM-Parametern, niemals Werte**. `lib/helmut/lambda-konfiguration.js` liest sie beim ersten Aufruf eines Containers mit `GetParameter` + `WithDecryption: true` (SecureString), setzt daraus `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` und `HELMUT_STORAGE_BACKEND=supabase` **nur im Prozessspeicher** und protokolliert sie nie — auch nicht gekürzt, auch nicht im Fehlerfall (AWS-Fehlertexte werden verworfen und auf fünf feste Ursachen abgebildet). **Fail closed:** fehlen, leeren oder scheitern die Parameter, wird NICHT gearbeitet und die Nachricht erneut zugestellt; ein stiller Rückfall auf den lokalen Speicher ist strukturell ausgeschlossen. Ein Fehlschlag wird nicht zwischengespeichert. Nachweis: `scripts/lambda-paket-test.js` §4–§7 (43 PASS). | lambda-konfiguration.js · **nirgends gesetzt** · nur in AWS-Lambda relevant · ohne sie arbeitet der Verbraucher gar nicht |
| `HELMUT_RELAY_FUNKTION` · `HELMUT_RELAY_MAX_LAEUFE` (Default 5) | O | **OP-30 Outbox-Relay (Korrekturlauf 2026-08-14/3).** `HELMUT_RELAY_FUNKTION` ist der Name der Relay-Lambda-Funktion, die der Verbraucher nach einem belegten Abschluss **asynchron** anstößt (`InvocationType: Event`) — genau EIN Anstoß je Verbraucherlauf, nie einer je Nachricht. Ist sie nicht gesetzt, läuft der Relay lokal im selben Prozess (Entwicklung/Test); geht ein Anstoß verloren, repariert ihn der minütliche Zeitgeber. `HELMUT_RELAY_MAX_LAEUFE` deckelt, wie viele Relayläufe ein **Zeitgeber**-Aufruf hintereinander macht (Schleife mit harter Obergrenze, keine Rekursion, keine Warteschleife). Nachweis: `scripts/outbox-relay-test.js` (37 PASS). | lambda-verbraucher.js/lambda-relay.js · **nirgends gesetzt** · nur in AWS-Lambda relevant |
| `HELMUT_ABRUF_LEASE_MS` (Default 60000) | O | **OP-30 Anbietersteuerung im Fachpfad (Korrekturlauf 2026-08-14/3).** Gültigkeitsdauer des Klassen-Slots, den ein laufender externer Abruf hält. Die Wache erneuert ihn währenddessen; scheitert die Erneuerung (Slot weggeräumt), bricht ein **echtes** Abbruchsignal die laufende Anfrage ab und der Auftrag wird vertagt — er arbeitet nie außerhalb seiner Grenze weiter. Nur wirksam mit `HELMUT_ANBIETER_STEUERUNG=on`. Nachweis: `scripts/anbietersteuerung-fachpfad-test.js` §5 (51 PASS). | crawler.js · **nirgends gesetzt** · Default greift · nur wirksam mit `HELMUT_ANBIETER_STEUERUNG` |
| `HELMUT_SELBSTWECK_ERLAUBT` | O | **Notfall-/Entwicklungsschalter.** Der Selbstweck (selbst gebauter HTTP-Weckruf) ist seit 2026-08-14 **nicht mehr** der Standardtransport. In einer Vercel-Production-Umgebung (`VERCEL_ENV=production`) ist er ohne `on` **gesperrt** — der Dispatcher stoppt geschlossen, statt still auf den schwächeren Weg zu fallen. Lokal und in Preview bleibt er ohne Schalter nutzbar. **Er ist Wert 4 von fünf für die Umschaltung auf den Ereignis-Antrieb (§7a)** — ohne ihn ist der Selbstweck-Weg in Production geschlossen, egal was sonst gesetzt ist. | job-dispatch.js · **nirgends gesetzt** |
| `HELMUT_ANBIETER_STEUERUNG` · `HELMUT_ANBIETER_<ANBIETER>_MINUTE` · `HELMUT_ANBIETER_<ANBIETER>_TAG` | O | **OP-30 verteilte Anbietersteuerung** (Migration `20260814090100`, Default AUS). Atomare Raten- und Tagesgrenzen je Anbieter/Modell/Klasse/Mandat mit Rückgabe des frühesten zulässigen Zeitpunkts, Vertagung statt Fehler, Schutzschaltung mit Erholung. Die Standardwerte (Google 30/min, OpenAI/Azure 20/min, Quelle 60/min, Tagesgrenzen 0 = nicht prüfen) sind **konservativ gewählt, keine Anbieterzusagen** — die Production-Werte sind vor einer Aktivierung anhand der echten Vertragsbedingungen zu entscheiden. Das **KI-Tagesbudget bleibt** bei `helmut_reserve_llm_call` (kein Doppelzählen). Fail closed: eine nicht prüfbare Grenze lässt nicht durch, sondern vertagt. | anbieter-steuerung.js · **nirgends gesetzt** |
| `HELMUT_KLASSEN_GRENZEN` (Default **AUS**) · `HELMUT_KLASSE_QUELLENABRUF_MAX` (Default 5) · `HELMUT_KLASSE_VERSTEHEN_MAX` (Default 1) · `HELMUT_KLASSE_WORKER_DRAIN_MAX` (Default 1) | O | **OP-30-Zielarchitektur — verteilte Arbeitsklassengrenzen** (instanzübergreifender Semaphor mit TTL-Selbstheilung, Migration `20260813090100_verteilte_grenzen.sql`, **seit 15.08.2026 angewendet** — frühere Angabe überholt). Fail closed in beide Richtungen: Flag aus ⇒ byte-identisches Verhalten; Flag an, Grenze nicht prüfbar ⇒ es wird NICHT gearbeitet (ehrliche Zurückstellung `klassengrenze-belegt`). Defaults erhöhen nichts: `quellenabruf` 5 = das bisherige prozesslokale Google-Gate, jetzt systemweit; `verstehen` 1 = die heutige globale Serialisierung, ehrlich verteilt (**>1 nur zusammen mit `HELMUT_VERSTEHEN_CAS`; ohne das Flag wird ein konfigurierter Wert >1 seit 2026-08-14 hart auf 1 GEKLEMMT und einmalig gemeldet — siehe die folgende Zeile**); `worker-drain` 1 = genau ein Wecksignal-Verbraucher gleichzeitig. Der Antrieb `ereignis` VERLANGT dieses Flag. Nachweis: `scripts/verteilte-grenzen-datenbank-test.js` (20). | scalable-pipeline.js/storage.js/server.js · **nirgends gesetzt** · Default AUS · — |
| `HELMUT_VERSTEHEN_KONKURRENZ` (Default **AUS**) · `HELMUT_VERSTEHEN_WACHE_TTL_MS` (Default 300000) | O | **OP-30-Zielarchitektur — Vorgangswache**, die kleinste sichere Ablösung des globalen Understanding-Schlosses: eine datenbank-atomare Exklusivbelegung je **Vorgang** (`helmut_klasse_belege`, Klasse `verstehen-vorgang:<id>`, max 1) unmittelbar vor dem Modellaufruf, Freigabe erst nach der Persistenz, TTL-selbstheilend bei Absturz. Wirkt in Cron- UND Warteschlangenpfad (`understanding.defaultDeps`); im Warteschlangenpfad ersetzt sie mit gesetztem Flag das globale Schloss für diesen Lauf. Fail closed: nicht prüfbare Wache erlaubt NICHTS (`skipped-cluster-belegt`, Dokumente bleiben Nachholkandidaten). Voraussetzung: Migration `20260813090100_verteilte_grenzen.sql`. Nachweis: `scripts/understanding-konkurrenz-test.js` (14). | storage.js/understanding.js/scalable-pipeline.js · **nirgends gesetzt** · Default AUS · — |
| `HELMUT_VERSTEHEN_CAS` (Default **AUS**) · `HELMUT_VERSTEHEN_PARALLELITAET` (Default 1, max 8) · `HELMUT_VERSTEHEN_LEASE_MS` (Default 300000) | O | **OP-30 — atomarer Verstehensvertrag (CAS)**, Migration `20260814180000_verstehen_cas.sql` (**nicht angewendet**). Löst den Karten-Store der Update-Vormerkungen (Lesen → Ändern → Schreiben, `CLAUDE.md` §4 Regel 10) durch **eine Zeile je Vorgang** mit Besitzerkennung, Lease und **monotonem Fencing-Wert** ab; der Fencing-Wert wird über `knowledge_objects.verstehen_fencing` per Trigger **erzwungen**, nicht nur geprüft. Ein Modellaufruf wird VOR dem Absenden vermerkt: ein Absturz danach endet sichtbar in `zustand='unbekannt'` und wird **nie** automatisch wiederholt (at most once). `HELMUT_VERSTEHEN_PARALLELITAET` begrenzt die gleichzeitig verstandenen Cluster **innerhalb eines Laufs** (1 = unverändert seriell); der Wert wird **ohne `HELMUT_VERSTEHEN_CAS` hart auf 1 geklemmt** und ist auf **8** gedeckelt — genau die lokal nachgewiesene Zahl. Dasselbe gilt für `HELMUT_KLASSE_VERSTEHEN_MAX` (systemweit). Fail closed an jeder Vertragsstelle: eine nicht prüfbare Zusage erlaubt weder Verstehen noch Speichern (`skipped-cluster-belegt` / `skipped-veraltet`). Nachweise: `scripts/verstehen-cas-datenbank-test.js` (68, echte PostgreSQL + echte Nebenläufigkeit), `scripts/verstehen-cas-vertrag-test.js` (68), `scripts/verstehen-cas-mutationsprobe.js` (6/6 rot). Kanonisch: [`op30-verstehen-cas-2026-08-14.md`](op30-verstehen-cas-2026-08-14.md). | understanding.js/verstehen-vertrag.js/storage.js/scalable-pipeline.js · **nirgends gesetzt** · Default AUS · — |
| `HELMUT_DRAIN_BUDGET_MS` (Default 60000) | O | **OP-30-Zielarchitektur — Zeitbudget eines Wecksignal-Verbraucherlaufs** (`/api/cron/worker-weck`), geklemmt 5 000–240 000 ms. Kurze Läufe + Kettenfortsetzung über die Folgeauftrags-Absichten statt eines langen Laufs; die Drain-Lease (`worker-drain`) läuft Budget+60 s. | server.js · **nirgends gesetzt** · Default 60 s · nur wirksam im Antrieb `ereignis` |
| `HELMUT_DEMAND_SHARED_WINDOW_H` (Default 8) · `HELMUT_DEMAND_PERSON_WINDOW_H` (Default 24) · `HELMUT_DEMAND_ARCHIVE_WINDOW_H` (Default 168) · `HELMUT_DEMAND_TENANT_MAX_AGE_H` (Default 24) | O | **OP-30 — Aktualitätsfenster des Source-Demand-Compilers**, nur wirksam bei gesetztem `HELMUT_SCALABLE_PIPELINE`. Das Fenster geht in den Idempotenzschlüssel ein: innerhalb eines Fensters entsteht je Abrufdefinition **genau ein** Auftrag, ein neues Fenster erzeugt einen neuen. `…SHARED…`: geteilte und globale Abrufe (8 h ⇒ 3×/Tag). `…PERSON…`: persönliche Aktuell-Suche — **höchstens 1× je 24 h** (Auftrag §7.5). `…ARCHIVE…`: persönliche Archivsuche `when:3m` — **höchstens 1× je 7 Tage** (§7.6); sie ist die teuerste Quellenklasse (offline gemessen ~98 Anfragen je Personenquelle). `…TENANT_MAX_AGE…`: Fenster für Mandatsprojektion und Briefingmaterialisierung; die Fälligkeiten werden deterministisch innerhalb des Fensters gestreut, damit nicht alle Profile gleichzeitig starten. **Fail closed:** ein gesetzter, aber unbrauchbarer Wert (leer, Text, negativ, unter dem Minimum) fällt auf den Standard zurück — nie auf „häufiger". Nachweis: `scripts/source-demand-test.js` §1. | source-demand.js · alle · Default greift · — |
| `HELMUT_RELEVANZORDNUNG` (Default **AUS**) | O | **OP-30 — zentrale Relevanzordnung der Nutzeransichten.** Setzt die Gründervorgabe **„Relevanz vor Aktualität"** um: Mandatsbezug mit Handlungsbedarf zuerst, dann Ausschuss und Region, dann dringende Bundes-/Landespolitik, dann allgemeine Relevanz — und **erst danach** Qualität, Aktualität und ein stabiler technischer Schlüssel. **Klassenbasiert, nicht punktebasiert.** **Anlass:** die Lage ordnete zuvor nach `created_at desc`; weil `now()` in einer Postgres-Transaktion konstant ist, war die Reihenfolge **undefiniert**. **DEFAULT AM 2026-08-08 VON AN AUF AUS GEÄNDERT** (Korrektursprint): ein default-AN-Flag hätte das Verhalten in dem Moment verändert, in dem gemergt wird — ein Merge ist ein Deployment (CLAUDE.md §5) —, nicht in dem Moment, in dem jemand es entscheidet. Damit gilt jetzt die Hausregel aller Flags (START_HERE §6). **Fail closed:** genau **ein** Wert schaltet ein (`on`); die bekannten Nein-Schreibweisen (leer, `off`, `aus`, `0`, `false`, `no`, `nein`) sind still AUS; **jeder andere Wert ist ebenfalls AUS und erzeugt einmalig eine Diagnose** (ein Tippfehler darf nicht wie eine Entscheidung aussehen). **Kein KI-Aufruf, kein Netz, rein deterministisch.** Nachweis: `scripts/relevanzordnung-test.js` (47) + `scripts/relevanzordnung-mergeneutralitaet-test.js` (24, vier unabhängige Beweise der Merge-Neutralität). | relevanzordnung.js/lage.js · **nirgends gesetzt** · Default AUS · — |
| `HELMUT_SCHUTZ_SIMULIERTE_UMGEBUNG` | O (nur lokal) | **OP-30 — eng begrenzte Ausnahme des lokalen Production-Schutzes.** Überspringt **ausschließlich die Umgebungsprüfung** von `scripts/lokaler-netzschutz.js`; die **Laufzeitsperre gegen nicht-lokale Verbindungen bleibt vollständig aktiv**. **Anlass:** drei Suiten (`restore-drill-test`, `backup-export-test`, `understanding-recovery-test`) bauen absichtlich eine Production-aussehende Umgebung auf, um die **Verweigerungslogik der Werkzeuge** zu beweisen — der Schutz beendete sie mit Exit 3, bevor das Werkzeug seine eigene Verweigerung mit Exit 2 zeigen konnte. Greift **nur** bei exakt `ja`. Der Runner setzt sie ausschließlich für die namentlich gelistete Allowlist. **Nie in Production setzen** — sie hat dort keine Wirkung, weil Production den Schutz gar nicht lädt. | lokaler-netzschutz.js · **nur im Test-Runner** · Default aus · — |
| `HELMUT_VORBEDINGUNG_WARTE_MS` | O | Wartezeit, bevor ein wegen fehlender Vorbedingung zurückgestellter Auftrag erneut fällig wird. Standard **120 000 ms (2 min)**, Minimum 10 s. Diese Zahl ist zugleich die **Granularität**, mit der eine Simulation den Betrieb abtasten muss — ein gröberer Takt misst eine Langsamkeit, die es nicht gibt (belegt 2026-08-08: ein Ein-Stunden-Takt ließ 200 Mandate scheinbar 25 statt 21:38 h brauchen). | scalable-pipeline.js · **nirgends gesetzt** · Default 2 min · nur wirksam bei `HELMUT_SCALABLE_PIPELINE` |
| `HELMUT_LLM_GLOBAL_ANTEIL` | O | Anteil des Tagesdeckels, der als **Reserve** für das globale Verstehen zurückgehalten wird; mandatsbezogene Arbeit darf ihn nie anfassen. Standard **0,5**. **Befund 2026-08-08:** der gemessene Bedarf ist zu **80–98 %** global (200 Mandate: 87,2 %). Bei starrer 50/50-Aufteilung muss der Deckel deshalb **1,6–2,0× größer** sein als der Bedarf, sonst hungert das Verstehen bei halb ungenutztem Mandatstopf. Zum Bedarf passend wären bei 200 Mandaten **0,87**. **Nicht gesetzt, nicht geändert** — wirksam ohnehin nur bei `HELMUT_LLM_FAIRNESS=on`. Rechnung: `scripts/skalierungsmodell.js#fairnessAufteilung`. | llm-budget-fair.js · **nirgends gesetzt** · Default 0,5 · nur wirksam bei `HELMUT_LLM_FAIRNESS` |
| `HELMUT_VORBEDINGUNG_MAX_WARTE_MS` | O | **OP-30 — Frist, nach der ein Briefing auch OHNE vollstaendige Vorbedingungen gebaut wird.** Standard 6 h, Minimum 60 s. **Anlass (belegter Produktfehler, 2026-08-08):** in der virtuellen 24-h-Simulation fuehrte ein vollstaendiger Google-Ausfall dazu, dass **0 von 200** Briefings entstanden — der Materialisierungsauftrag wartete unbegrenzt auf Verstehensergebnisse, die nie kamen. Kein Briefing ist schlechter als ein ehrlicher Leerzustand. Die Frist wird gegen `created_at` gemessen, **nicht** gegen `due_at`: `helmut_defer_job` schiebt `due_at` selbst nach vorn, eine Frist gegen `due_at` koennte deshalb nie ablaufen (ebenfalls am 2026-08-08 belegt und behoben). Nachweis: `scripts/skalierung-simulation-test.js`. | scalable-pipeline.js · **nirgends gesetzt** · Default 6 h · nur wirksam bei `HELMUT_SCALABLE_PIPELINE` |
| `HELMUT_BUDGET_WARTE_MS` | O | **OP-30 — Wartezeit, bevor ein wegen erschoepftem KI-Tagesbudget zurueckgestellter Auftrag es erneut versucht.** Standard 1 h, Minimum 60 s. **Anlass (belegte Messung, 2026-08-08):** ohne diese lange Zurueckstellung erzeugten 200 Mandate an EINEM simulierten Tag **38 549 abgelehnte Reservierungen** — Leerlauf, der die Warteschlange belastet, ohne je Arbeit zu leisten. Kuerzer waere genau dieser Leerlauf, laenger liesse freiwerdendes Budget (Tageswechsel, zurueckgegebene Reservierung) zu lange ungenutzt. Nachweis: `scripts/skalierung-simulation-test.js`. | scalable-pipeline.js · **nirgends gesetzt** · Default 1 h · nur wirksam bei `HELMUT_SCALABLE_PIPELINE` |
| `HELMUT_BUDGET_MAX_WARTE_MS` | O | **OP-30/O5 — OBERGRENZE des Budgetwartens.** Standard **48 h**, Minimum 1 h. **Anlass (Befund O4 des Abschlussreviews 2026-08-08, behoben am 2026-08-09):** `helmut_defer_job` nimmt den Versuch ausdrücklich zurück, es gab also **keine** Zählung, die das Zurückstellen je beendet hätte. Bei dauerhaft erschöpftem Tagesbudget pendelte ein Verstehensauftrag **unbegrenzt** zwischen `wartend` und `läuft` — er erzeugte in jedem Durchlauf Datenbanklast und hielt einen Rückstand offen, der nie kleiner wurde. Nach dieser Frist bricht der Auftrag mit `budget-dauerhaft-erschoepft` ab; der Text gilt als **endgültig** und wird nicht fünfmal wiederholt. 48 h und nicht 24: erst wenn **zwei volle Tagesbudgets** nichts frei gemacht haben, ist das kein Warten mehr, sondern ein Zustand. Gemessen ab `created_at`, nicht ab `due_at` (gleiche Begründung wie eine Zeile darüber). Nachweis: `scripts/op30-aktivierungsreife-test.js` §4. | scalable-pipeline.js · **nirgends gesetzt** · Default 48 h · nur wirksam bei `HELMUT_SCALABLE_PIPELINE` |
| `HELMUT_WIEDERVORLAGE_STUNDEN` | O | **OP-30/O5 — Mindestalter, bevor ein endgültig gescheiterter Auftrag wieder vorgelegt wird.** Standard 24 h, Minimum 1 h. Kürzer würde eine echte Störung sofort wiederholen, statt ihr Zeit zu geben, sich zu erledigen. Migration `20260809_jobqueue_wiedervorlage.sql` (**nicht angewendet**). | scalable-pipeline.js · **nirgends gesetzt** · Default 24 h · nur wirksam bei `HELMUT_SCALABLE_PIPELINE` |
| `HELMUT_WIEDERVORLAGE_MAX` | O | **OP-30/O5 — wie oft ein endgültig gescheiterter Auftrag höchstens wieder vorgelegt wird.** Standard 2, `0` schaltet die Wiedervorlage ab. **Anlass (Befund O5):** der Idempotenzschlüssel des Verstehens trägt bewusst kein Fenster, und `helmut_jobs_bereinigen` räumt `fehlgeschlagen` ausdrücklich nicht weg — eine gescheiterte Dokumentmenge wäre damit **dauerhaft** blockiert gewesen, nach einer einzigen vorübergehenden Modellstörung. Die Wiedervorlage löscht den Fehlerbeleg **nicht**; sie ist begrenzt, weil eine Dokumentmenge, die auch beim dritten Anlauf scheitert, ein Befund für den Betreiber ist und keine Arbeit für den Worker. Was übrig bleibt, meldet `helmut_jobs_blockiert` und macht `betriebsstatus` **kritisch**. Nachweis: `scripts/jobqueue-wiedervorlage-datenbank-test.js` (echte PostgreSQL). | scalable-pipeline.js · **nirgends gesetzt** · Default 2 · nur wirksam bei `HELMUT_SCALABLE_PIPELINE` |
| `HELMUT_LLM_FAIRNESS` | O | **OP-30 — faire, ergebnisbezogene KI-Budgetschicht.** **Code-Default: AUS**, fail closed (nur `on`/`true`/`1`/`an`). Bewusst ein ZWEITES Flag neben `HELMUT_SCALABLE_PIPELINE`: dieses schaltet die Warteschlange ein, jenes die Budgetschicht darüber. Ein gemeinsames Flag wäre bei einem Fehler nicht auswertbar. Ist es an, wird vor jedem KI-Aufruf des Warteschlangenpfads eine **ergebnisbezogene** Reservierung gezogen (`helmut_reserve_llm_result`): eine Wiederholung desselben Auftrags kostet dann **nichts** zusätzlich, und Mandantenanteil plus globales Notfalllimit werden in **einem** atomaren Schritt geprüft. Reicht das Budget nicht, wird der Auftrag **ehrlich zurückgestellt** (bleibt fällig) statt als erledigt gemeldet. **Erhöht keinen Deckel** — die Schicht kann nur weniger zulassen, nie mehr. **Voraussetzung:** Migrationen `20260717_llm_budget_reservation.sql` **und** `20260808_llm_budget_fairness.sql`; fehlt eine, meldet die Schicht `migration-fehlt` und der bestehende Deckel im V3-Pfad greift unverändert. Nachweis: `scripts/llm-budget-fairness-test.js`. **Production-Zustand: nicht gesetzt (= aus).** | scalable-pipeline.js/llm-budget-fair.js · **nirgends gesetzt** · Default greift · — |
| `HELMUT_LLM_GLOBAL_ANTEIL` (Default 0.5) | O | **OP-30 — Anteil des KI-Tagesbudgets, der der GLOBALEN Arbeit (Understanding) vorbehalten bleibt.** Nur wirksam bei gesetztem `HELMUT_LLM_FAIRNESS`. Begründung: ein Understanding-Aufruf nützt allen Mandaten zugleich und darf von mandatsbezogener Arbeit nicht verdrängt werden — dieselbe Überlegung wie die bestehende Understanding-Reserve, nur als Anteil, damit sie mit dem Deckel mitwächst. Der Rest wird gleichmäßig auf die fälligen Mandate verteilt. **Fail closed:** nur ein Wert echt zwischen 0 und 1 zählt; alles andere fällt auf 0.5 zurück. Nachweis: `scripts/llm-budget-fairness-test.js` A5. | llm-budget-fair.js · alle · Default greift · — |
| `HELMUT_UNDERSTANDING_BUENDEL` (Default 25) | O | **OP-30 — wie viele Dokumentkennungen in EINEN Verstehensauftrag gebündelt werden.** Nur wirksam bei gesetztem `HELMUT_SCALABLE_PIPELINE`. Klein genug, dass ein Auftrag in seinem Zeitbudget bleibt; groß genug, dass nicht je Artikel ein Auftrag entsteht. Der Auftragsschlüssel ist der **Inhaltsfingerabdruck** der Kennungsmenge — dieselben Artikel erzeugen nie einen zweiten Auftrag, auch nicht in einem neuen Aktualitätsfenster. Nachweis: `scripts/v3-anbindung-test.js` §1/§2. | scalable-pipeline.js · alle · Default greift · — |
| `HELMUT_VORBEDINGUNG_WARTE_MS` (Default 120000) | O | **OP-30 — wie lange ein zurückgestellter Auftrag wartet, bevor er erneut fällig wird.** Nur wirksam bei gesetztem `HELMUT_SCALABLE_PIPELINE`. Betrifft Projektion und Briefing, solange Abruf oder Verstehen im selben Aktualitätsfenster noch offen sind, sowie Aufträge, deren KI-Budget gerade erschöpft ist. **Zurückstellen ist kein Fehlversuch**: `helmut_defer_job` nimmt den Versuch zurück, damit reines Warten einen gesunden Auftrag nicht nach `max_attempts` in den Fehlertod treibt. Untergrenze 10 s. Nachweis: `scripts/v3-anbindung-test.js` §4/§5. | scalable-pipeline.js · alle · Default greift · — |
| `HELMUT_JOB_TIMEOUT_MS` (Default 120000) | O | **OP-30 — Zeitgrenze EINES Auftrags**, nur wirksam bei gesetztem `HELMUT_SCALABLE_PIPELINE`. Ohne sie könnte der Worker sein Laufzeitbudget nicht halten: `crawlAllSources` hat je **einzelner** Anfrage ein Timeout (`CRAWLER_TIMEOUT_MS`), aber **keine** Gesamtgrenze — Befund **F-REQ**, einzelne Production-Quellen liefen 41 892 ms bei 7 000 ms Anfragelimit und `retry_count = 0` ([`vorgangskontext.md`](vorgangskontext.md) §7.6). Die Grenze ist zusätzlich durch die **Restzeit** des Workers gedeckelt. **Ehrliche Einschränkung:** `Promise.race` beendet die ursprüngliche Promise nicht (wie beim äußeren Cron-Zeitlimit, R-6) — der Auftrag läuft im Hintergrund weiter, wird aber **nicht** als erledigt gemeldet, sondern als vorübergehend gescheitert; die Lease läuft aus und er kehrt zurück. Minimum 5 000 ms. | scalable-pipeline.js · alle · Default greift · — |
| `HELMUT_WORKER_BATCH` (Default 10) | O | **OP-30 — Zahl der Aufträge, die ein Worker je Runde reserviert**, nur wirksam bei gesetztem `HELMUT_SCALABLE_PIPELINE`. Größer = weniger Reservierungsrunden, aber gröbere Zeitsteuerung; kleiner = feinere Steuerung bei mehr Round-Trips. **Ändert kein Zeitbudget** — der Worker hält sein `budgetMs` unabhängig von diesem Wert ein und schließt einen begonnenen Auftrag nie über die Abschlussreserve hinaus ab. | server.js · alle · Default greift · — |
| `HELMUT_WORKER_PARALLEL` (Default 2, 1–8) · `HELMUT_WORKER_BUDGET_MS` (Default 240000, 5 000–900 000) · `HELMUT_WORKER_LEASE_MS` (Default 120000, 10 000–900 000) · `HELMUT_WORKER_STAPEL` (Default 10, 1–200) | O | **OP-30 — Grenzen des eigenständigen Workerbetriebs** (`lib/helmut/worker-betrieb.js` `grenzenAusEnv`). Alle vier sind **hart auf die genannten Bereiche geklemmt**; ein ungültiger oder nicht-positiver Wert fällt auf den Standard zurück, nie auf „unbegrenzt". **Nachgetragen im Abschlussreview 2026-08-08:** sie fehlten im Inventar, weil der Vollständigkeitsscanner in `scripts/env-inventar-test.js` nur `process.env.NAME`, `flagValue("NAME")` und `env.NAME` sieht — `worker-betrieb.js` liest über einen eigenen Helfer. Der Scanner kennt die vier Namen jetzt ausdrücklich. **Stand 2026-08-09 berichtigt:** die frühere Angabe „wird von `server.js` und keinem Cron aufgerufen" ist seit PR #236 **überholt** — `workerBetrieb.durchlauf` ist in den Narrativslots verdrahtet (`server.js narrativSlotLauf`), und `runCronUeberWarteschlange` ruft es ebenfalls. Die Grenzen wirken damit **im Cron**, nicht erst in einem eigenständigen Workerprozess. Die Morgenslots übergeben eigene Werte (`budgetMs` 270 000, `leaseMs` 300 000, `leerlaufWarteMs` 20 000) — ein Aufrufer kann die harten Bereiche dabei nie überschreiten. | worker-betrieb.js · **nirgends gesetzt** · Default greift · — |
| `HELMUT_NARRATIV_PARALLEL` (ohne Vorgabe: es gilt `HELMUT_WORKER_PARALLEL`; hart 1–8) | O | **OP-30/Kapazität — Parallelität NUR der Morgenslots** (`server.js narrativSlotGrenzen`, Kapazitätssprint 2026-08-09). **Warum getrennt von `HELMUT_WORKER_PARALLEL`:** der allgemeine Standard ist bewusst klein (2), weil `source_fetch` gegen die Google-Drosselung läuft — dort schaden mehr Worker. Das Lage-Narrativ ruft kein Google an, es wartet auf das Modell. Diese Variable hebt deshalb ausschließlich die Slots `lage-briefing` und `lage-briefing-nachlauf` an, ohne die Crawl-/Pipeline-Slots zu berühren. Die harte Klemmung 1–8 aus `worker-betrieb.grenzenAusEnv` gilt unverändert; ein Aufrufer kann sie nicht umgehen. **Für Stufe 200 ist der Zielwert 8** — Beleg [`op30-kapazitaet-morgenslots-2026-08-09.md`](op30-kapazitaet-morgenslots-2026-08-09.md). Wirkungslos ohne `HELMUT_SCALABLE_PIPELINE` + `HELMUT_NARRATIV_QUEUE`. | server.js · **nirgends gesetzt** · Default greift · — |
| `HELMUT_NARRATIV_TIMEOUT_MS` (Default 45000, min 5 000) | O | **OP-30/Kapazität — eigene Zeitgrenze des Auftragstyps `tenant_narrative`** (`scalable-pipeline.TYP_ZEITGRENZE_MS`). Sie kann `HELMUT_JOB_TIMEOUT_MS` nur **senken**, nie erhöhen (`Math.min`), und gilt ausschließlich im Warteschlangenpfad — der Altpfad kennt sie nicht. **Warum 45 s:** 120 s sind für einen Quellenabruf richtig (Kette aus 37–98 Anfragen, Befund F-REQ), für ein Narrativ zu großzügig; ein hängender Auftrag verbraucht sonst mehr als die Hälfte eines Slotbudgets. Aus der Production-Messreihe (`llmUsage`, n = 134, 2026-07-10 bis 2026-08-09): Median 5 033 ms, p95 32 201 ms, Maximum 471 985 ms — und **von den acht Aufrufen über 20 s war kein einziger erfolgreich**, der langsamste erfolgreiche lag bei 20 012 ms. 45 s sind mehr als das Doppelte davon und kosten in der gemessenen Reihe **kein** Narrativ. | scalable-pipeline.js · **nirgends gesetzt** · Default greift · — |
| `HELMUT_WORKER_LEERLAUF_MS` (Default **0 = aus**, 0–30 000) | O | **OP-30/Kapazität — Leerlaufwarten eines Workers** (`worker-betrieb.grenzenAusEnv`, `scalable-pipeline.arbeite`). Default 0 bedeutet **unverändertes Verhalten**: findet ein Worker nichts Fälliges, endet der Slot sofort. Ist ein Wert gesetzt, wartet ein Worker, der **selbst** Arbeit zurückgestellt oder wiederholt hat, so lange und schaut noch einmal nach — solange sein Slotbudget das trägt. Wer nichts zurückgestellt hat, wartet nie; ein leerer Morgen kostet keine zusätzliche Ausführungszeit. **Anlass:** der Wiederholungs-Backoff beginnt bei 15–30 s; ein Auftrag, der gegen Slotende scheitert, wäre sonst erst im 16:00-Slot dran gewesen. Die Morgenslots übergeben 20 000 ms direkt (nicht über die Umgebung). | worker-betrieb.js/scalable-pipeline.js · **nirgends gesetzt** · Default AUS · — |
| `HELMUT_RAW_DOCUMENT_BULK_CHUNK` (Default 200) · `HELMUT_FINDING_COUNT_CHUNK` (Default 100) | O | Blockgrößen des Rohdokument-Schreibpfads (`persistRawDocumentsDeduped`, Quellenmodus `on`). Anlass ist der **Kapazitätsbefund F-RT** vom 2026-08-03: der Pfad schrieb **je Dokument einen eigenen Round-Trip** — gemessen 616 Einzel-Upserts in 89,89 s plus ~108 × (GET+PATCH) für `finding_count` in 34,85 s, zusammen **834 Requests / 124,74 s = 46,7 %** eines 267-s-Laufs. Jetzt gehen die Zeilen blockweise hinaus. `…BULK_CHUNK`: Zeilen je Bulk-Upsert; schlägt ein Block fehl, wird **genau dieser Block** einmal einzeln nachgezogen (bisherige Robustheit erhalten). `…FINDING_COUNT_CHUNK`: Kennungen je `id=in.(…)`-Filter beim **bedingten** `finding_count`-Update (Compare-and-Set auf den gelesenen Stand, CLAUDE.md §4.10). **Beide ändern keine fachliche Wirkung** — es werden dieselben Zeilen mit denselben Werten geschrieben. Kleiner setzen = mehr Requests, größer setzen = größere Anfragekörper. | storage.js · alle · Default greift · — |
| `DIP_WAHLPERIODE` (Default 21) · `HELMUT_DIP_PRIMARY` · `DIP_CACHE_MS` | O | DIP-Verhalten. | dip.js · alle · Default greift · — |
| `AUTO_REFRESH_ON_READ` | O | Lazy-Refresh beim Lesen. | server.js · alle · Default greift · — |
| `HELMUT_MANUAL_RUN_MIN_INTERVAL_MS` | O | Drossel für manuelle Läufe. | server.js · alle · Default greift · — |

## 5. Lage / Radar / Darstellung

| Variable | P/O | Zweck / Default | Merkmale |
|---|---|---|---|
| `HELMUT_KO_SCAN_LIMIT` | O | KO-Scan-Fenster. Default 500. | storage.js · alle · Default greift · — |
| `HELMUT_LAGE_MAX_VORGAENGE` · `HELMUT_LAGE_DEMO` · `HELMUT_LAGE_CHECK_*` · `HELMUT_LAGE_CHECK_SOURCE_LIMIT` | O | Lage-/Lage-Check-Verhalten (Demo default aus). | lage.js · alle · Default greift · — |
| `HELMUT_SCORING_MODE` | O | Ebenen-bewusstes Ranking + gap/stale/quiet. Default off. Dynamisch gelesen (`env.HELMUT_SCORING_MODE`). | scoring.js · alle · Default off · — |
| `HELMUT_V3_MATCHING` / `HELMUT_MATCHING_DIM` / `HELMUT_V3_LAZY_UNDERSTANDING` / `HELMUT_V3_OFFICE` / `HELMUT_V3_SHADOW_COMPARE` | O | V3-Subsysteme, Default aus (`HELMUT_V3_SHADOW_COMPARE` dynamisch gelesen). **`HELMUT_V3_MATCHING` ist in Production faktisch AN** — belegt über frische `profile_embeddings`/`matching_results` (Sprint 22A, Befund E-1, 2026-07-28); der Wert selbst ist aus einer Cloud-Sitzung nicht lesbar. `HELMUT_MATCHING_DIM` MUSS ungesetzt bleiben (Default 256 = `vector(256)`-Spalte); ein abweichender Wert erzeugte write-time Vektoren, die nicht in die Spalte passen. | storage.js/office.js/supply-shadow-compare.js · alle · Default aus · — |
| `HELMUT_MATCHING_AUDIT` | O | **Rollout-Grenze der Matching-Auditpersistenz** (Sprint 23B-1, Roadmap-Punkt 23). **Default AUS.** Aus = exakt das bisherige Verhalten: kein Zugriff auf `matching_runs`, keine Sperre, keine Audit-Spalten in `matching_results`, keine neue Fehlerquelle. An = jeder Lauf wird protokolliert, identische Läufe schreiben nichts mehr, der Lage-Pfad nimmt dieselbe Sperre wie der Crawl-Pfad. **Voraussetzung: Migration `20260728_matching_audit` angewendet** — ohne sie schlagen die Audit-Schreibvorgänge fehl. Zusätzlich an `v3StoreReady()` gekoppelt (ohne Supabase-Store bleibt der Pfad inert). Aktivierung ist eine Freigabeentscheidung. | storage.js/matching.js/matching-audit.js · alle · Default aus · — |
| `HELMUT_MATCHING_RELEVANZ_GATE` | O | **Relevanzriegel im Matching** (Sprint M-8, Befund M-8). **Default AUS.** Aus = exakt das bisherige Verhalten: die Top-N der RPC werden unbedingt gespeichert. An = veröffentlicht wird nur, was dem Mandat gegenüber begründbar ist (belegte `matched_features`); es wird **nicht** aufgefüllt, keine Mindestmenge erzwungen und **nichts** neu berechnet (Ähnlichkeit, Rang, Reihenfolge, Ergebniskennung unverändert). Bewusst **kein** Ähnlichkeitsschwellenwert — Begründung und Production-Messung: `docs/matching-nachvollziehbarkeit.md` Teil E. Keine Migration nötig. Aktivierung ist eine Freigabeentscheidung und verändert sichtbare Lage und Briefings. | matching.js/matching-relevanz.js/storage.js · alle · Default aus · — |
| `HELMUT_FRESH_STALE_HOURS` · `RADAR_DYNAMICS_FRESH_DAYS` · `HELMUT_TOP_TOPIC_COOLDOWN_HOURS` · `HELMUT_DECISION_LABEL_MAX` · `HELMUT_RAWDOC_SUMMARY_MAX` (Default 240) | O | Frische-/Anzeigeparameter. | radarState.js/storage.js · alle · Default greift · — |
| `HELMUT_REVIEW_FIXTURE` | O | **Gefährlich in Prod:** liefert fiktive Abnahmedaten. MUSS in Production ungesetzt sein. | reviewFixture.js · nur Dev · Default sicher (aus) · — |

## 6. Watchdog / Betrieb / Store

| Variable | P/O | Zweck / Default | Merkmale |
|---|---|---|---|
| `HELMUT_MIN_CHECKED_SOURCES` · `HELMUT_MIN_SUCCESSFUL_SOURCES` · `HELMUT_MIN_CONFIGURED_SOURCES` · `HELMUT_MIN_LAGE_CHECK_SOURCES` | O | Quellenabdeckung-/Watchdog-Schwellen. **P2-5:** Defaults auf die relationale Architektur kalibriert (120/110/120/75; gesunder Ist-Crawl ~145), zentral in `lib/helmut/source-coverage.js`. | server.js · alle · Default greift · — |
| `HELMUT_MAX_CRAWL_FAILURE_RATIO` · `HELMUT_MAX_FULL_CRAWL_AGE_MS` · `HELMUT_MAX_LAGE_CHECK_AGE_MS` · `HELMUT_MAX_OUTPUT_FRESHNESS_MS` | O | Frische-/Fehler-Grenzen des Watchdogs. | server.js · alle · Default greift · — |
| `HELMUT_STORAGE_BACKEND` · `HELMUT_SUPABASE_STORE_ID` (Default `main`) · `HELMUT_SUPABASE_AUTH_STORE_ID` · `HELMUT_STORE_CACHE_MS` · `HELMUT_SUPABASE_TIMEOUT_MS` | O | Store-Backend/Caching. | storage.js · alle · Default greift · — |
| `HELMUT_BLOB_RETRY_MAX` (Default 2) · `HELMUT_BLOB_RETRY_BASE_MS` (Default 250) | O | **P0-5 Stufe 1.** Begrenzter Retry mit exponentiellem Backoff für Blob-Reads/Writes bei transienten Fehlern (Timeout/5xx/Verbindung; 4xx werden nicht geretryt). Nach erschöpften Versuchen: ein technischer `systemError` (kein stiller Lauf-Verlust). Feste Obergrenze — keine Endlosschleife. | storage.js · alle · Default greift · — |
| `HELMUT_CRAWL_RUN_RETENTION` (Default 20) | O | **P0-5 Stufe 1.** Aufbewahrte Crawl-Läufe im Blob (an `saveCrawlRun` angeglichen, non-lossy). Die eigentliche Blob-Entlastung liefert Stufe 2 (Crawl-Läufe relational, freigabepflichtig). **Production seit 2026-08-06: `36`** — ausschließlich für Production gesetzt (OP-25-Betreiberschritt K3; Mindestbedarf 30 bei n=5, ab 36 keine Knapp-Warnung), wirksam mit Redeploy `dpl_3y5nBCiQtHnUnVuqh1SFr2X2ranu` (READY 07:50:22 UTC, Commit `f4f4500b`). Wert durch **direkte Sichtprüfung des Betreibers** in Vercel bestätigt; aus Claude-Sitzungen wegen der Egress-Sperre nicht über die Vercel-API lesbar (Betreiberbestätigung, keine API-Einsicht). Zuvor griff der Code-Default 20 (Blob hielt exakt 20 Laufzeilen). | storage.js · alle · Production `36` (2026-08-06), sonst Default 20 · — |
| `HELMUT_OP25_AKTIVIERUNG_AT` · `HELMUT_OP25_KOSTENRAHMEN_USD` (Default 2) | O | **OP-25-Nachweiswerkzeug (rein lesend, 2026-08-04).** Aktivierungszeitpunkt des globalen Abrufs (ISO, alternativ CLI `--aktivierung`) und dokumentierter LLM-Kostenrahmen je 24-h-Fenster für `scripts/op25-production-nachweis.js` (Herleitung: Baseline 0,20 USD/24 h × 10; Vertrag `betrieb/vorgangskontext.md` §7.7). Beide wirken NUR im Nachweiswerkzeug, nie in der Laufzeit; ohne Aktivierungszeitpunkt meldet das Werkzeug ehrlich `noch_nicht_auswertbar`. | scripts/op25-production-nachweis.js · Auswertung · fail-closed (`noch_nicht_auswertbar`/`blockiert`) · — |
| `HELMUT_CRAWL_RUNS_RELATIONAL` | O | **P0-5 Stufe 2, FREIGABEPFLICHTIG, Default AUS.** Dual-Write der Crawl-Läufe in die relationale Tabelle `crawl_runs` (Migration `20260720` — NICHT auf Prod angewendet). Ohne Flag UND ohne Migration = No-Op. Übergangsplan: `docs/betrieb/blob-relational-migration-plan.md`. | storage.js · Prod nach Freigabe · fail-open (No-Op) · — |
| `HELMUT_PROCESS_RUNS_RELATIONAL` | O | **W-2 Werkzeug-Härtung, FREIGABEPFLICHTIG, Default AUS.** Dual-Write der Prozess-Lauftelemetrie in die relationale Tabelle `process_runs` (Migration `20260727` — **am 2026-07-27 freigegeben und auf Prod angewendet**, CLAUDE.md §5; die frühere Angabe „nicht angewendet" hier war veraltet); atomarer Upsert je `(run_id, process)`, dadurch parallel-sicher und idempotent (der Blob verliert Läufe per Last-Write-Wins, Befund W-2). Ohne Flag/Migration bleibt der Blob-Pfad aktiv — mit sichtbaren Fehlern statt `.catch(() => {})`. Übergangsplan: `docs/betrieb/blob-relational-migration-plan.md`. | storage.js · Prod nach Freigabe · fail-safe (Blob-Fallback, Fehler sichtbar) · — |
| `HELMUT_RETENTION_EXECUTE` | O | **Datenschutz-Retention, FREIGABEPFLICHTIG, Default AUS.** Erlaubt dem Retention-Werkzeug (`scripts/retention-dryrun.js --execute`) die ECHTE Löschung überalteter `raw_documents`/`knowledge_objects` nach der Datenklassen-Matrix. Ohne Flag ist nur der Trockenlauf möglich. Löscht Prod-Daten (kaskadiert Provenienz) → Gründer- + Rechtsfreigabe. Konzept: `docs/betrieb/aufbewahrung-loeschung.md`. | storage.js/retention.js · Prod nach Freigabe · Default aus (nur Trockenlauf) · — |
| `HELMUT_SOURCE_TELEMETRY` | O | **P0-1, FREIGABEPFLICHTIG, Default AUS.** Aktiviert den Write der Pro-Quellenabruf-Telemetrie in die relationale Tabelle `source_crawl_telemetry` (Migration `20260718_source_crawl_telemetry.sql` — NICHT auf Prod angewendet). Ohne Flag UND ohne Migration = reiner No-Op (kein Production-Daten-Write). Nur technische Metadaten, kein Volltext/PII. | scheduler.js/source-telemetry.js · Prod nach Freigabe · fail-open (No-Op) · — |
| `HELMUT_RECOVERY_EXECUTE` (+ `HELMUT_RECOVERY_CONFIRM`, `HELMUT_RECOVERY_RUNID`) | O | **STILLGELEGT (2026-07-18, Pending-Sprint) — wirkungslos.** Der anker-basierte Recovery-Pfad erzeugte einen Multi-Themen-Digest (Lauf `rec-29569461715`, vollständig zurückgerollt) und wurde hart stillgelegt: `scripts/understanding-recovery-execute.js` ist ein reiner Stilllegungs-Hinweis (kein DB-/KI-/Write-Pfad, unabhängig von Flag/Token), die `RECOVERY_ALLOWLIST` ist leer, die GitHub-Action `understanding-recovery.yml` wurde entfernt. Ersatz: Einzel-Dokument-Recovery je exakter `raw_document_id` (Restliste OP-05). Historie: `docs/betrieb/understanding_recovery_trockenlauf.md`. | understanding-recovery.js · — · wirkungslos (No-Op) · — |
| `HELMUT_PENDING_TERMINAL_EXECUTE` · `HELMUT_PENDING_TERMINAL_CONFIRM` · `HELMUT_PENDING_TERMINAL_RUNID` | O | **OP-06 Terminales Aussortieren, FREIGABEPFLICHTIG, Default AUS.** Schaltet das Aussortier-Skript (`scripts/pending-terminal-aussortieren.js`) für genau die 34 bestätigten Allowlist-Fälle (27 Rauschen + 7 belegte Themen-Duplikate) scharf; zusätzlich exaktes Token `AUSSORTIEREN_34_BESTAETIGT` nötig. Ohne beides: reiner Plan-/Snapshot-Ausdruck (read-only). Echter Lauf: konditionale PATCHes `pending`/`failed` → `failed-final` mit Rollback-Kennung `aussortiert:<RUNID>:<vorstatus>` in `understanding_model`; 0 KI-Calls, kein Delete. Freigabevorlage: `docs/betrieb/pending_terminal_aussortierung.md`. | pending-terminal.js · Prod nach Freigabe · fail-safe (No-Op) · — |
| `VERCEL_REGION` · `HELMUT_EXEC_LOCATION` | O | Ausführungsort-Etikett für die Laufzeit-Telemetrie (P0-1, technische Metadaten, nie PII). `VERCEL_REGION` wird von Vercel gesetzt; `HELMUT_EXEC_LOCATION` überschreibt es lokal. Default `local`. | scheduler.js/server.js · alle · Default greift · — |
| `HELMUT_PIPELINE_ERROR_WINDOW_MS` (Default 6 h) | O | **P0-3.** Dedup-Fenster des zentralen Pipeline-Fehler-Sammlers: identische Fehler (gleicher Prozess+Fehlertyp+Quelle) innerhalb dieses Fensters erhöhen einen Zähler statt neue `systemErrors`-Zeilen zu fluten. | storage.js · alle · Default greift · — |
| `HELMUT_COMMIT_SHA` (auch `VERCEL_GIT_COMMIT_SHA`) | O | Commit-Kennung für die Fehler-/Telemetrie-Korrelation (technische Metadaten). `VERCEL_GIT_COMMIT_SHA` wird von Vercel gesetzt. | storage.js · Prod · Default greift · — |
| `HELMUT_MONITORING_WEBHOOK_URL` | O | **Zweiter Alarmkanal** (Audit-Folgebranch): Health-Report wird zusätzlich zu CallMeBot als JSON-POST hierher geschickt (Slack/Discord/Zapier/E-Mail-Relais). Ohne: nur WhatsApp. Setzen = **F5 (freigabepflichtig)**; Härtung (Ereigniskennung, Dedupe, Retry, Zustellstatus, Heartbeat) siehe `docs/betrieb/f5_freigabe.md`. | server.js/monitoring-webhook.js · Prod empfohlen · fail-open (Kanal still aus) · Rot: bei Webhook-Leak |
| `HELMUT_MONITORING_WEBHOOK_RETRY_MAX` | O | Max. Wiederholungen der Webhook-Zustellung bei Netzfehler/5xx (Default 2, exponentieller Backoff; 4xx wird nie wiederholt). Harte Obergrenze, keine Endlosschleife. | monitoring-webhook.js · alle · Default greift · — |
| `HELMUT_ASSET_VERSION` | O | Cache-Busting-Version für den CLI-Deploy-Weg (setzt `scripts/vercel-deploy.sh` aus Git-SHA+Zeit). Git-Integration-Deploys nutzen stattdessen `VERCEL_GIT_COMMIT_SHA`. **CLI-Deploys IMMER über das Skript** (siehe `deploy-rollback.md`). | server.js · nur CLI-Deploy · ohne: Konstante (Stale-Asset-Falle) · — |
| `HELMUT_LAGE_CHECK_RECENT_HOURS` · `HELMUT_LAGE_CHECK_REGENERATE_THRESHOLD` | O | Lage-Check-Feinsteuerung. | lage.js · alle · Default greift · — |
| `HELMUT_STAFF_STALE_DAYS` | O | Staff-Backfill-Frische. | staff.js · alle · Default greift · — |
| `HELMUT_BRIEFING_FRISCHE` | O | **Not-Aus des Frischevertrags** (Default AN). Nur `off`/`0`/`false`/`aus`/`nein` schaltet ab; dann entfällt die Aussage „Briefing noch nicht aktuell“ und der Kopf verhält sich wie vor dem Vertrag. Kein Aktivierungsschalter — Abschalten ist eine Betreiberentscheidung mit Ehrlichkeitsverlust. | briefing-frische.js/server.js · alle · Default greift (AN) · — |
| `HELMUT_BRIEFING_MAX_DATENALTER_H` (Default 24) | O | Ab welchem Alter des jüngsten verstandenen Vorgangs die Daten als „nicht ausreichend aktuell“ gelten (Frischevertrag Punkt 5). | briefing-frische.js · alle · Default greift · — |
| `HELMUT_BRIEFING_VORABEND_H` (Default 8) | O | Wie weit das Briefingfenster vor Tagesbeginn reicht, wenn kein vorheriger erfolgreicher Lauf bekannt ist (16:00 Berliner Vorabend) — damit späte Vorabendmeldungen im Morgenbriefing „neu“ sind. | briefing-frische.js · alle · Default greift · — |
| `HELMUT_BRIEFING_RELEVANZ_TAGE` (Default 14) | O | Grenze zwischen „weiterhin relevant“ und „Hintergrund“ für ältere Vorgänge. | briefing-frische.js · alle · Default greift · — |
| `HELMUT_BRIEFING_MAX_RUECKBLICK_TAGE` (Default 3) | O | Wie weit der letzte erfolgreiche Lauf zurückliegen darf, um noch den Fensteranfang zu bestimmen; danach greift der Vorabend-Standard (kein Riesenfenster nach einem Ausfall). | briefing-frische.js · alle · Default greift · — |

## 7. Pflicht-Mindestset für einen funktionierenden Production-Neuaufbau

`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET`, `PILOT_SECRET` (oder
`HELMUT_AUTH_MODE=accounts` + Admin-Seed), `AZURE_OPENAI_ENDPOINT`+`AZURE_OPENAI_KEY`
(oder `OPENAI_API_KEY`), `HELMUT_V3_STORE=1`, `HELMUT_SOURCE_MODE=on`,
`HELMUT_MAX_LLM_CALLS_PER_DAY`. Für Push zusätzlich die VAPID-Trias, für
Alarmierung `CALLMEBOT_*` (bzw. `HELMUT_MONITORING_WEBHOOK_URL`, Zweitkanal).
Ein Startup-Warnhinweis bei fehlenden Pflichtvariablen ist als spätere
Ergänzung sinnvoll (Roadmap), aber kein Blocker.

## 7a. Umschaltung auf den Ereignis-Antrieb: **fünf** Werte, nicht drei

**Korrektur vom 2026-08-24.** Mehrere Dokumente nannten für die Umschaltung von `shadow` auf
den echten Warteschlangenbetrieb **drei** Werte (`HELMUT_JOB_DISPATCH_MODE=queue`,
`HELMUT_KLASSEN_GRENZEN=on`, `HELMUT_WORKER_WAKE_URL`). Das war seit dem Härtungssprint
2026-08-14 falsch: seitdem ist der **Standardtransport `sqs`**, und der Selbstweck ist in einer
Vercel-Production-Umgebung ohne ausdrückliche Freischaltung gesperrt. Mit nur drei Werten wäre
der Transport `sqs` gewählt, die Queue-Adresse fehlte, und es würde **nichts** zugestellt.

Für den Selbstweck-Weg sind es nach heutigem Code **genau fünf** Werte:

| # | Variable | Wert | Warum |
|---|---|---|---|
| 1 | `HELMUT_JOB_DISPATCH_MODE` | `queue` | Ereignis-Antrieb statt Cron-Antrieb |
| 2 | `HELMUT_KLASSEN_GRENZEN` | `on` | der Ereignis-Antrieb **verlangt** die verteilten Grenzen (sonst 409 auf jedes Wecksignal) |
| 3 | `HELMUT_JOB_TRANSPORT` | `selbstweck` | ohne diesen Wert greift der Standardtransport `sqs` |
| 4 | `HELMUT_SELBSTWECK_ERLAUBT` | `on` | in `VERCEL_ENV=production` sonst geschlossen gesperrt |
| 5 | `HELMUT_WORKER_WAKE_URL` | `https://<production-host>/api/cron/worker-weck` | Weckziel; unterliegt dem Ziel-Riegel |

**Bereits vorhandene Voraussetzungen** (keine neuen Änderungen, nur zu prüfen):
`HELMUT_SCALABLE_PIPELINE=on` (Production seit 23.08.2026), `CRON_SECRET` gesetzt, die
Plattform-Systemvariablen `VERCEL_PROJECT_PRODUCTION_URL`/`VERCEL_URL`/`VERCEL_BRANCH_URL`
(setzt Vercel selbst; sie sind der Vertrauensanker des Weckziels) und die angewendeten
OP-30-Migrationen.

**Vorprüfung statt Vermutung:** `GET /api/ops/jobqueue` (mit `CRON_SECRET`) liefert seit
2026-08-24 das Feld `ereignisbetrieb` — es unterscheidet *angeforderten Modus*, *wirksamen
Modus*, *Antrieb*, *gewählten Transport*, *Verfügbarkeit*, *Grund* und `bereit`. **Der
Ereignisbetrieb gilt erst dann als bereit, wenn `ereignisbetrieb.bereit === true` ist** —
`antrieb: "ereignis"` allein ist ausdrücklich **kein** Bereitschaftsbeleg. Die Ausgabe enthält
weder Secrets noch Adressen noch Hostnamen (testgesichert:
`scripts/jobdispatch-vertrag-test.js` §13.10).

**Kostenaussage (ehrlich):** Der Selbstweck braucht **keinen neuen Anbieter und keine neue
feste Infrastrukturgebühr**. Er erzeugt aber **zusätzliche Vercel-Funktionsaufrufe, zusätzliche
Rechenzeit und zusätzliche Speicherbelegung**; die tatsächliche Mehrbelastung der Rechnung ist
ohne Prüfung von Tarif, Kontingenten und aktueller Nutzung **unbekannt**
(https://vercel.com/docs/functions/usage-and-pricing). „Kostet null" ist keine zulässige
Aussage.

## 8. Werkzeug-/Script-Variablen (nicht Laufzeit)

Diese Variablen liest NICHT der Server, sondern Betriebs-Skripte bzw.
GitHub-Actions-Workflows. Sie gehören nicht nach Vercel (Ausnahmen vermerkt),
sondern in die Shell des Betreibers, `.env.local`, GitHub Secrets/Variables —
**oder, für Claude-Code-Cloud-Sitzungen, die Claude-Code-Environment-Einstellungen**
(Environment → Environment Variables).

**Vierter Kanal — Claude Code Cloud (seit 2026-07-25, `CLAUDE.md` §4.9):**
Produktionsrelevante Skripte, die Secrets benötigen, müssen sowohl lokal als auch in
einer Cloud-Sitzung lauffähig sein. Eine Cloud-Sitzung läuft in einem isolierten,
frisch geklonten Container ohne Zugriff auf die lokale Maschine des Betreibers und
ohne persistenten Zustand über einen Neustart hinaus — eine `.env.local` kann dort
nicht wie gewohnt abgelegt werden. Secrets erreichen den Prozess einer Cloud-Sitzung
stattdessen als echte `process.env`-Variablen, sobald sie einmal in den
Environment-Einstellungen der jeweiligen Claude-Code-Umgebung hinterlegt sind —
analog zu `GITHUB_TOKEN`, das dort bereits ohne jede manuelle Eingabe verfügbar ist.
**Niemals** Secrets in den Chat einer Sitzung eingeben oder in einen Commit
aufnehmen. Technische Voraussetzung (bereits erfüllt, geprüft 2026-07-25): kein
Skript in diesem Repo lädt `.env.local` selbst per `dotenv` — jedes liest Secrets
ausschließlich über `process.env`, ist also gegenüber dem Herkunftskanal
(Shell-Export, `.env.local`, GitHub Secret, Cloud-Environment-Variable) blind und
funktioniert identisch, unabhängig davon, welcher der vier Kanäle die Variable
gesetzt hat.

| Variable | Ort (Lesestelle) | Zweck / Hinweise |
|---|---|---|
| `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` (S) | `scripts/backup-export.js` | Pre-Seed-/Voll-Backup (read-only). Läuft identisch lokal (`.env.local`) und in einer Claude-Code-Cloud-Sitzung (Environment Variables der Cloud-Umgebung) — beide Kanäle landen gleichermaßen in `process.env`. |
| `TARGET_SUPABASE_SERVICE_ROLE_KEY` (S) | `scripts/restore-drill.js` | Service-Role-Key des ZIEL-Testprojekts für die Restore-Übung (die Ziel-URL kommt als CLI-Argument `--target-url`, nicht als Env). Nur ad hoc in der Shell setzen, nie persistieren. Darf NIE der Production-Key sein. |
| `VERCEL_TOKEN` (S) | vercel-CLI (`scripts/vercel-deploy.sh` setzt Login via `vercel whoami` voraus) | CLI-/CI-Zugang zum Vercel-Account. Wird von keinem Repo-Code direkt gelesen; Erzeugung/Widerruf unter vercel.com → Account → Tokens. Rotationspflichtig (secret-rotation.md). **Wichtig — der Token allein genügt nicht (gemessen 2026-07-26, erneut 2026-08-03):** in einer Claude-Code-Cloud-Sitzung sperrt die Egress-Richtlinie `api.vercel.com`, `vercel.com` und `mcp.vercel.com` (`CONNECT → HTTP 403`). Die Vercel CLI liest den Token zwar selbstständig aus `process.env`, scheitert damit aber schon bei einem **rein lesenden** `project ls` (`fetch failed`); der Vercel-MCP-Server ist authentifiziert (Teams, Projekte, Deployments, Runtime-Logs), hat aber **kein** Environment-Werkzeug und **keinen** Redeploy. **Eine Vercel-Env-Änderung ist deshalb nur möglich, wenn Token UND geöffneter Egress zu `api.vercel.com` zusammenkommen — sonst bleibt jede Flag-Aktivierung Betreiberaktion über die Vercel-Oberfläche.** Belege: [`berlin-aktivierung.md`](berlin-aktivierung.md) §20.3 (`HELMUT_LANDESMODULE`, sechs Kanäle), [`vorgangskontext.md`](vorgangskontext.md) §7.3 (`HELMUT_CRON_GLOBALABRUF`). Ein Umgehen der 403-Ablehnung ist untersagt und wurde nicht versucht. |
| `HELMUT_CRON_SECRET` (S, GitHub-Secret) | `.github/workflows/briefing-watchdog.yml` | GitHub-Seite der `CRON_SECRET`-Doppelpflege — MUSS wertgleich mit `CRON_SECRET` in Vercel sein, sonst schlägt der Watchdog fehl. |
| `HELMUT_PROD_URL` (GitHub-Variable, `vars.`) | `.github/workflows/briefing-watchdog.yml` | Basis-URL für den Watchdog-Aufruf. Default `https://helmut-pilot.vercel.app`. |
| `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `OPENAI_API_KEY` / `AZURE_OPENAI_KEY` / `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_DEPLOYMENT` (S, GitHub-Secrets/Vars) | `.github/workflows/staff-backfill-*.yml` | GitHub-Spiegel der Vercel-Werte für die manuellen Staff-Backfill-Workflows. Bei Rotation der Vercel-Werte MITROTIEREN. |
| `HELMUT_BASE_URL` | `scripts/smoke-test.js` | Ziel-URL des Smoke-Tests (Default lokal). |
| `HELMUT_SMOKE_AUTH` | `scripts/smoke-test.js` | `1` = Account-Modus-Smoke (Login/RBAC/Mandantentrennung) statt Pilot-Smoke. |
| `HELMUT_SMOKE_ADMIN_EMAIL` / `HELMUT_SMOKE_ADMIN_PASSWORD` (S bei Passwort) | `scripts/smoke-test.js` | Admin-Zugang für den Account-Modus-Smoke (Fallback: `HELMUT_ADMIN_EMAIL`/`HELMUT_ADMIN_PASSWORD`). |
| `HELMUT_UNDERSTANDING_EVAL_LIVE` | `scripts/understanding-eval.js` | `1` = Goldset-Eval mit ECHTEN KI-Calls (Kosten!). Default offline. Steht auch in `.env.example`. |

Bewusst NICHT inventarisiert (reine Test-/Probe-Knöpfe ohne Betriebsrelevanz):
`PARDOK_*` (pardok-shadow-test), `PP_*` (pardok-structure-probe), `S9B_*`
(sprint9b-verify), `SP_*` (shadow-pilot-crawl), `TMPDIR`.

## 9. Veraltete Variablen

Diese Variablen existieren noch als Lesestellen bzw. historische Referenzen,
haben aber keine Wirkung mehr. NICHT neu setzen; bei Gelegenheit aus Vercel
entfernen.

| Variable | Status |
|---|---|
| `HELMUT_TENANT_JWT_MODE` | Stillgelegt: `tenantJwtModeEnabled()` liefert hart `false`. Setzen ändert nichts. |
| `SUPABASE_JWT_SECRET` | Nur vom stillgelegten Tenant-JWT-Modus konsumiert — aktuell wirkungslos. |
| `SUPABASE_ANON_KEY` | Nur vom stillgelegten Tenant-JWT-Modus konsumiert — aktuell wirkungslos. |
| `HELMUT_MONITORING_EMAIL` | Hat NIE existiert (Doku-Phantom bis 2026-07). Der Zweitkanal heißt `HELMUT_MONITORING_WEBHOOK_URL`. |

## 10. Kritische Production-Variablen

Kritisch = Fehlen oder Fehlwert bricht Production sofort sichtbar ODER
entfesselt Kosten. Diese Werte bei JEDER Env-Änderung gegenprüfen:

| Variable | Warum kritisch |
|---|---|
| `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` | Ohne beide: kein Store — App startet, zeigt aber keine Daten; Crons schreiben ins Leere. |
| `CRON_SECRET` | Ohne: alle `/api/cron/*` antworten 503 → kein Crawl, kein Briefing, kein Health-Report (fail-closed). Falscher Wert nur in GitHub (`HELMUT_CRON_SECRET`): Watchdog schlägt dauerhaft fehl. |
| `PILOT_SECRET` (Legacy-Modus) | Ohne: niemand kommt in die App (fail-closed). |
| `HELMUT_MAX_LLM_CALLS_PER_DAY` | Falsch hoch gesetzt = Kostenrisiko. Fehlend/ungültig = Schutzlimit 50 → Understanding-Läufe werden gedrosselt (fail-closed, kein Kostenrisiko, aber Funktionseinbuße). |
| `HELMUT_LLM_BUDGET_FAIL_CLOSED` | In Prod = 1. Ohne: bei Budget-Prüffehlern werden KI-Calls ERLAUBT (fail-open → Kostenrisiko bei Störung). |
| `HELMUT_SOURCE_MODE` | Ohne/off: kein Quellen-Cutover — Crawl liefert keinen neuen Stoff, App veraltet still. |
| `AZURE_OPENAI_ENDPOINT`+`AZURE_OPENAI_KEY` (oder `OPENAI_API_KEY`) | Ohne: nur Regel-Fallbacks, keine KI-Ausgaben (App läuft, Kernnutzen fehlt). |
| `HELMUT_V3_STORE` | Ohne `=1`: V3-Pfade inert — Understanding/Büro/Radar-Datenfluss steht. |
| `HELMUT_REVIEW_FIXTURE` | MUSS ungesetzt sein — gesetzt liefert es fiktive Daten an echte Nutzer. |
